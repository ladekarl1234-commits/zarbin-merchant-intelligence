# Expert judging panel — the record

> The measured before/after (retrieval, data correctness, latency) is in
> **[EVALUATION.md](EVALUATION.md)**. This document is the panel: sixteen independent
> expert lenses, their scores, and every critical/high finding that survived being
> handed to a separate agent briefed to refute it.

> Machine-generated from `docs/evaluation_rounds.json` by `pipeline/gen_evaluation.py`.
> Every number below comes from the archive; the two clearly-labelled prose sections at
> the end are hand-written.

## Headline

| | Round 1 — as deployed, before panel fixes | change |
|---|---|---|
| Commit | `5acf9e1` | |
| Date | 2026-08-22 | |
| Lenses | 16 | |
| **Mean dimension score** | **69.1** / 100 | **0.0** |
| **Competition rubric** | **256** / 300 | **0.0** |
| Findings raised | 141 | |
| — critical / high | 10 / 42 | |
| Critical+high confirmed after adversarial re-check | 17 | |
| — refuted | 23 | |

## The competition rubric, criterion by criterion

| Criterion | Max | R1 | change |
|---|---:|---:|---:|
| Actionability & novelty of insights | 90 | 81 | 0.0 |
| Correctness & traceability | 75 | 65 | 0.0 |
| Analytical depth | 60 | 44 | 0.0 |
| Non-technical UX | 45 | 38 | 0.0 |
| Technical quality & executability | 30 | 28 | 0.0 |
| **Total** | **300** | **256** | **0.0** |

## Dimension scores

| Lens | Dimension | R1 | change | confidence (last) |
|---|---|---:|---:|---|
| `rubric-official` | Official competition rubric (300 pts) | 85 | 0.0 | high |
| `architecture` | Technical architecture & engineering quality | 81 | 0.0 | high |
| `code-quality` | Code quality & maintainability | 76 | 0.0 | high |
| `data-correctness` | Data & analytics correctness | 79 | 0.0 | high |
| `statistics` | Statistical methodology rigor | 56 | 0.0 | high |
| `security` | Security & privacy | 78 | 0.0 | high |
| `reliability` | Reliability, error handling & observability | 61 | 0.0 | high |
| `scalability` | Scalability & performance | 65 | 0.0 | high |
| `product` | Product quality | 73 | 0.0 | high |
| `business` | Business viability & value proposition | 66 | 0.0 | high |
| `ux` | UX & usability (non-technical merchant) | 66 | 0.0 | high |
| `design` | Visual & interaction design, Persian/RTL craft | 68 | 0.0 | high |
| `accessibility` | Accessibility & inclusive design | 64 | 0.0 | high |
| `ai-grounding` | AI quality, grounding & AI-Ops | 62 | 0.0 | high |
| `retrieval-eval` | Retrieval quality & evaluation methodology | 58 | 0.0 | high |
| `testing-qa` | Testing & quality assurance | 68 | 0.0 | high |

## What each expert concluded (round 1)

### Official competition rubric (300 pts) — 85/100

*Lens `rubric-official` · confidence high*

A=81/90 C=65/75 D=44/60 U=38/45 T=28/30 TOTAL=256/300. Up 20 from the previous panel's 236. I verified all three of the previous rubric-lens correctness defects are genuinely closed, not just marked closed: across 100 live merchants / 272 cards there are 0 impact_high > realized GMV (7 capped and disclosed with capped:true plus a Persian cap caveat); the drawer's opportunity formula and its printed computation are now the same expression with the same operands; ZB-120's ntile determinism holds empirically (6 repeated local generate() runs x 6 merchants produced 0 differing numeric leaves — only computed_at timestamps move); and /api/quality now computes both 1 and 28 with text explaining the nesting. Grain reconciles independently to the row: attempts mart 1,949,353 + no_attempt 263,936 = 2,213,289 (the claimed 2.21M raw rows), sessions 2,062,839 with 2,062,839 distinct session_keys, and /api/quality, /api/admin/platform and the marts agree on verified GMV to the rial (5,165,464,690,799); the 28-session anomaly exactly explains session-grain 1,025,655 vs attempt-grain 1,025,627. The copilot rebuild is the biggest single gain: the regex router went 31.8% -> 95.4% exact on a 129-question holdout written after the router was frozen, and calibrate_nlu.py provably never touches it. Held back by two live traceability defects on the two surfaces built to prove traceability (the hours intent attaches no_attempt_rate evidence whose SQL reads a mart with no hour column; Control Center shows the payment source's freshness as 2018-10-20 for 2026 data), by the copilot silently answering day-of-week questions with hour-of-day numbers, and by depth gaps that are verbatim unchanged: zero statistical uncertainty anywhere in the backend, and dow / win_bank / init_time_ms each appear exactly once in the whole backend (pipeline.py) and drive no analysis.

**Not higher because:** Not 90+ (270/300) because two defects sit directly on the product's headline traceability claim and both are live right now: clicking «این عدد از کجا آمد؟» on the hours answer shows a metric the answer never mentions, computed from merchant_daily which has no hour column — claim 1 is false on 1 of 13 intents; and the Control Center's data-sources page, whose entire job is "is the data current?", reports the ZarinPal source as last-updated 2018-10-20 because it reads the marts file mtime instead of the latest data timestamp its own base.py comment promises. Claim 7 ("answers the question that was asked, or refuses and says why") also fails on day-of-week: two of four dow phrasings route to `hours` and confidently return hour-of-day numbers rather than refusing. Analytically, the depth ceiling the previous panel named has not moved — no Wilson interval, bootstrap, standard error or p-value exists in any backend module (the opportunity band is still a fixed 0.5/0.75/1.0 scenario multiplier, honestly labelled but not inference), and word-boundary search shows dow, win_bank and init_time_ms each occur exactly once in 5,684 backend lines, all in pipeline.py: three dimensions materialized at build time and analysed nowhere. Actionability still lacks any what-if or did-it-work loop, and 6 of 9 generators emit constant action_fa strings.

**Not lower because:** Not below 75 (225/300) because every headline claim I could falsify, I tried to falsify and it held. Determinism: I diffed 6 consecutive local generate() runs for 6 merchants leaf-by-leaf — the only differing leaves were computed_at timestamps, zero numbers moved. GMV cap: 0 violations in 272 cards across 100 live merchants, with the 7 capped cards clamped to exactly realized GMV and flagged. Cross-surface consistency: three independent endpoints and the marts agree on session count and verified GMV to the last rial, and the 28-anomaly reconciles the two grains exactly — that is a real single semantic layer, not three code paths that happen to look similar. The retry-inflation invariant holds (recovered 39,658 vs multi-try verified 41,911, so paid-after-retry is correctly excluded). WCAG: I recomputed --ink-3 #5f616c on #f6f6f7 by hand and got 5.70:1, matching the comment in theme.css exactly — ZB-035 is genuinely fixed, not annotated. The routing eval is methodologically honest in a way most submissions are not: a dev set and a post-freeze holdout, with calibrate_nlu.py restricted to leave-one-out over the bank, and it still scores 95.4% on 129 unseen questions. 180 backend tests in 4.61s, 43 frontend tests in 1.34s (the previous panel's one named tech gap, closed with a test targeting exactly the flagged cross-language duplication), ruff clean, 403 on unauthenticated admin, and measured cold server-side medians of 27.8-88.2ms across five merchant endpoints.

**Independently verified strengths:**

- Determinism is real, not asserted. I diffed 6 consecutive local `insights.generate()` runs for 6 merchants leaf-by-leaf: the ONLY differing leaves were `computed_at` timestamps — zero numeric values moved, including for M250 and M18 which carry high_value_friction and psp_friction (the two ntile cards ZB-120 was about). Both windows now read `ntile(5) OVER (ORDER BY amount, session_key)` (insights.py:312) and `ORDER BY amount, session_key, try_seq` (insights.py:436).
- The realized-GMV cap now covers every generator, verified at scale: 100 live merchants, 272 cards, 0 with impact_high > realized GMV. 7 cards were capped, each clamped to exactly realized GMV, each carrying `capped: true` and a Persian caveat naming the cap («هر برآورد ریالی حداکثر تا کل فروش موفق همان دوره سقف‌گذاری می‌شود»). M31's inbank_gap would have published 275.6B against 114.5B of realized sales; it is clamped and relabelled as a funnel alert instead of money.
- The grain claim reconciles to the row across four independent surfaces. Attempts mart 1,949,353 + no_attempt sessions 263,936 = 2,213,289, matching the stated 2.21M raw rows; sessions = 2,062,839 with 2,062,839 DISTINCT session_keys; /api/quality outcome counts sum to 2,062,839 and /api/admin/platform reports the same; verified GMV is 5,165,464,690,799 in both. Session-grain verified (1,025,655) minus attempt-grain verified (1,025,627) = 28, exactly the `verified_wo_verified_try: 28` anomaly the quality page discloses.
- The routing rebuild is measured against a genuine holdout, and the calibration provably cannot see it. `retrieval.py:158` defines SETS = {dev: RETRIEVAL_CASES, holdout: HOLDOUT_CASES}; `pipeline/calibrate_nlu.py:24/42` scores leave-one-out over the intent bank only. Running the eval: holdout (129 questions written after the router was frozen) goes 0.318 → 0.954 exact, misroute 0.528 → 0.022, with the hardest families moving most (colloquial 0.231 → 1.000, paraphrase 0.200 → 0.960). Probing 13 canonical intents live, 13/13 routed correctly and 5/5 out-of-scope probes (forecast, PII, FX price, greeting, prompt injection) were refused.
- The WCAG fix is arithmetically correct, not just annotated. theme.css:13 claims --ink-3 #5f616c gives 5.70:1 on --bg #f6f6f7; I computed the relative luminances by hand (0.1206 vs 0.9223) and got (0.9223+0.05)/(0.1206+0.05) = 5.70 — exact. --ink-4 is demoted to decorative with a do-not-use-for-text comment. Alongside this: 43 frontend tests now run in 1.34s including fmt.ts↔fa.py parity, closing the previous panel's one named technical deduction with a test aimed at the exact duplication that was flagged.

### Technical architecture & engineering quality — 81/100

*Lens `architecture` · confidence high*

I built the import graph by AST-walking all 37 modules in zarin/: it is acyclic at module scope, and the only lower-to-higher edges are 5 deliberate function-local imports in db.invalidate_derived plus 2 in sources.base.registry. The layering claims mostly survive adversarial checking — copilot.py issues zero SQL (0 q(/q1( call sites), so the "one deterministic engine" story is structural rather than aspirational; middleware order is correct for the stated reason; all 13 f-string SQL sites interpolate only int(limit) or an allow-listed token. The DuckDB/parquet choice holds up under measurement, not just prose: peak working set across all 10 endpoints including /api/quality's 1.95M-row attempts group-by is 97 MB, and forced-CDN-MISS /api/overview?m=M156 returns in 35-52 ms of Server-Timing app;dur. Two things keep this out of the top band. The response-cache middleware sits above the FastAPI dependency graph, so with ZARIN_REQUIRE_AUTH=1 — the documented production switch and the recorded fix for ZB-001 — an anonymous request to a warmed URL returns 200 with the full tenant body, and that body carries Cache-Control: public, s-maxage=31536000; cache.py's own docstring reasons about exactly this hazard for /api/admin/* and got it right there. And the two largest deploy artifacts (public/, 1.1 MB SPA; data/marts/, 63 MB) are both gitignored and built by neither CI job, with public/ a manual unverified cp -r copy of the tracked zarin/static/ — and public/ is the copy the CDN actually serves.

**Not higher because:** Two structural gaps a production fintech cannot carry. (1) The multi-tenancy seam does not exist in the only configuration where it does anything: cache.middleware short-circuits the _merchant_scope Depends, so the ZB-001 fix recorded as "fixed in main" is inert under ZARIN_REQUIRE_AUTH=1, and the same responses are marked public, s-maxage=31536000 for a shared CDN. (2) There is no automated path from a green CI to a correct deployment: CI runs ruff+pytest+tsc and builds neither the marts nor the CDN bundle, and the CDN bundle is an untracked hand-synced duplicate — a frontend rebuild that skips step 2 of the DEPLOY_VERCEL recipe makes local (zarin/static) and production (public/) diverge with local testing still passing. Secondary: the ops surface runs a second, unmeasured 18-branch regex router with a silent catch-all while the merchant surface runs measured retrieval, and the invalidate_derived cache registry that memory.md calls a correctness invariant has zero test coverage.

**Not lower because:** I hunted for the usual rot and did not find it. No import cycle (AST graph over 37 modules). No god module — largest is insights.py at 713 loc, and at ruff's default C901 threshold of 10 the entire ~4,100-line backend has exactly 2 offenders. No SQL injection despite 13 f-string query sites — every user value goes through DuckDB $name binding. No duplicated metric computation: copilot.py has 0 q(/q1( calls and composes analytics/insights/peers. No memory cliff from the 63 MB in-bundle parquet (97 MB peak RSS). The abstractions that exist are three typing.Protocols and a dispatch dict, and I confirmed each is actually used rather than decorative — including copilot.py:464's import-time assert set(_ANSWER) == set(nlu.BANK), which makes router/answer drift structurally impossible. Runtime dependency surface is 2 Python packages and 4 npm packages. Every defect above is a missing guard on a sound structure, not a design that needs unwinding.

**Independently verified strengths:**

- Import graph is acyclic at module scope. I AST-walked all 37 modules in zarin/ (pipeline/_panel/impgraph.py), separating module-level from function-local imports: the only lower-to-higher edges are the 5 in db.invalidate_derived and 2 in sources.base.registry, both deliberate and commented. No cycle anywhere else.
- The 'one deterministic engine' claim is structural, not aspirational: grep -cE '\bq1?\(' zarin/copilot.py -> 0. The copilot writes no SQL at all; it composes analytics/insights/peers. Contrast api.py (9), insights.py (10), analytics.py (14).
- copilot.py:464 `assert set(_ANSWER) == set(nlu.BANK), "every retrievable intent needs an answer, and vice versa"` — an import-time invariant that makes router/answer-table drift structurally impossible. The module fails to import rather than routing to a missing handler.
- No SQL injection despite 13 f-string query sites. I read every one: api.py:418-426 interpolates {cond} gated by _VALID_OUTCOMES and {int(limit)} behind Query(ge=1,le=50); control.py:124-136 interpolates {order} from _MERCHANT_SORT.get(sort, default) with a second allow-list check at api.py:355; peers.py:51 interpolates a where built from a literal tuple. Every user value binds via DuckDB $name.
- The 63 MB in-bundle parquet does not become a serverless memory problem: measured peak working set across all 10 endpoints — including /api/quality's full `session_key IN (SELECT ... FROM attempts GROUP BY 1 HAVING sum(ok::int)=0)` over 1.95M rows — is 97 MB (15 MB at start, 61 MB after import). Cold db.connect() incl. 5 views + schema guard is 22 ms on top of a 92 ms duckdb import. Forced-CDN-MISS live: /api/overview app;dur 35-52 ms, /api/insights 170-189 ms.

### Code quality & maintainability — 76/100

*Lens `code-quality` · confidence high*

10,751 source lines (5,675 Python in zarin/, ~3,000 TSX, 180 pytest + 43 vitest) for a full-stack payments-analytics product — the codebase is smaller than the problem, not larger. I ran an AST reachability scan over all 189 top-level definitions in zarin/ and found zero dead code (the 13 "unreferenced" names are all FastAPI handlers bound by decorator). Median Python function is 8 lines across 285 functions; only 3 exceed cyclomatic complexity 8. Zero TODO/FIXME/HACK; exactly 3 broad `except Exception` repo-wide, 2 of them in untracked scratch. The failure mode is not bloat, it is that module docstrings assert stronger invariants than the code enforces, and the *second* implementation of anything never receives the fix the first one got: ops_copilot.py claims "same two-stage contract as the merchant copilot" but is still the pre-rewrite regex ladder with a terminal else, answering «سلام» and a prompt-injection probe with a platform summary at confidence=high; registry.py claims "nothing is hand-written per UI card" while three hand-maintained Persian label tables disagree on 4 of 5 metrics; registry.evidence's docstring defines sql_kind="query" as "the literal query that ran" and three call sites pass reconstructions, one against a table that was never queried. The fa.py↔fmt.ts duplication is diverging on live data right now: 2 of 343 merchants render their conversion rate differently on the backend than the frontend, verified end-to-end on the deployment.

**Not higher because:** Three contracts stated in module docstrings are falsified by the code directly beneath them (ops_copilot.py's "same two-stage contract", registry.py's "nothing is hand-written per UI card", registry.evidence's sql_kind="query" = "the literal query that ran") — in a product whose entire pitch is traceability, a docstring that describes a stronger invariant than the code holds is the most expensive kind of comment rot. The one genuinely unavoidable cross-language duplication (Persian formatting) is observably diverging on real merchant data, the parity test written to catch it has no tie cases, and CI never runs it. A second copilot exists that never got the architectural lesson its sibling's docstring spends a paragraph explaining, with the fix (`copilot._OUT_OF_SCOPE_C`) importable and unused two files away.

**Not lower because:** Every mechanical measure of maintainability I could run came back clean and I ran them rather than assuming: zero dead code across 189 definitions, median function 8 lines, `tsc --noEmit --noUnusedLocals --noUnusedParameters` silent under strict mode, no TODOs, no swallowed exceptions, no `any` outside a browser SpeechRecognition shim, `pipeline/_*` gitignored so 20+ scratch experiments never entered the repo. Duplication is already factored out where it pays — `insights._gap_card` backs three 10-line wrappers, `copilot._ANSWER` is dict dispatch bound by `assert set(_ANSWER) == set(nlu.BANK)`. Comments are 7.7% of lines and the ones I read explain WHY with issue IDs attached (zone-map pruning, byte-reproducible sort keys, ZSTD-vs-Snappy), not what. The 220-line `pipeline.build` is a linear ETL script that would be worse if split. This is a codebase someone maintained deliberately; the defects are drift between two implementations, not neglect.

**Independently verified strengths:**

- Zero dead code, measured not assumed: I parsed every zarin/**/*.py with ast, collected all 189 top-level function/class definitions, and counted textual references across zarin/, tests/, pipeline/, frontend/src/ and docs/. Only 13 came back unreferenced and every one is a FastAPI route handler bound by decorator (admin_ai_eval, get_insights, evidence_sessions, …). No orphan helpers, no abandoned abstractions.
- Function size and complexity are genuinely under control: across 285 Python functions the median is 8 lines; `ruff --select C901 --config lint.mccabe.max-complexity=8` flags exactly 3 (gen_expert_review.render, gateway.grounding_failure, ops_copilot._plan). The largest function, pipeline.build at 220 lines, is a linear ETL script whose comments explain parquet zone-map pruning, a tiebreaker sort key for byte-reproducible rebuilds, and the ZSTD-over-Snappy size tradeoff — splitting it would add indirection for nothing.
- Error handling is not papered over: `except Exception` appears 3 times in the entire repo, 2 of them in untracked pipeline scratch, and the one in shipped code (zarin/obs.py:32) carries a comment explaining why the telemetry middleware must catch and re-raise. Zero TODO/FIXME/HACK/XXX across zarin/, frontend/src/ and tests/; the only debt markers are two `ponytail:` comments that name their ceiling (db.py's fixed thread budget, InsightCard's un-synced localStorage).
- Duplication is already factored out where it actually pays: insights._gap_card (67 lines) backs three 10-line `_card_*_gap` wrappers rather than three copies; copilot dispatches 13 intents through an `_ANSWER` dict closed by `assert set(_ANSWER) == set(nlu.BANK)`, so an intent can never exist without an answer; analytics._AGG_SQL is one string shared by the query and three evidence payloads. Repo hygiene matches: `pipeline/_*` is gitignored, so 20+ scratch experiments (_nlu_tail.py, _copilot_plan.py, a whole duplicate parquet set in _mz/) sit on disk and never entered git — only 7 pipeline files are tracked.
- Frontend type discipline is real: tsconfig sets `strict: true` and `noUnusedLocals: true`, and I ran `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` — clean, so there are no unused parameters either. `any` appears only in Copilot.tsx around the untyped `window.SpeechRecognition` browser API. Page components share one `useData` hook plus Loading/Empty/Section/EvBtn primitives, so per-page loading and error boilerplate is two lines, identical everywhere.

### Data & analytics correctness — 79/100

*Lens `data-correctness` · confidence high*

I recomputed the whole chain from the 2.21M-row raw CSV and the computation layer holds up: 2,213,289 attempt rows → 2,062,839 sessions, and a FULL JOIN of per-merchant sessions/verified/GMV between an independent raw recomputation and merchant_daily returns zero mismatched rows across all 343 merchants. The six outcomes partition sessions exactly (1,025,655 + 733,603 + 263,936 + 30,938 + 8,706 + 1 = 2,062,839, no NULLs), and no session contradicts settled_at/verified_at. LMDI closes to 2.3e-13 relative and the Δconv driver identity to 1.1e-16. Every one of 343 merchants × 3 windows respects the realized-GMV cap per card — 0 violations — and a 600-window funnel fuzz found no broken denominator, no rate outside [0,1], no non-monotonic stage. Where it breaks is scope, not arithmetic: two surfaces of the same product answer the same question with different money. The Changes page splits the default 181-day window 91/90 while the GMV-change insight card splits it 90/90, so M156 shows ΔGMV = 470,641,036,190 on one screen and 479,703,376,180 on the other (9.06bn IRR apart, verified live). The Control Center's merchant table discards f/t entirely and prints lifetime figures under a windowed header — for June, M156's row shows 61,847,264,950 IRR unverified against a platform-wide windowed total of 19,610,888,186, i.e. one merchant at 315% of the whole platform. repeat_gap mixes lifetime transaction volume with a period-scoped cap, so on a 7-day window 26 of 38 such cards (68%) clamp to exactly 100% of the week's GMV.

**Not higher because:** Three separate places where a correct number is shown in the wrong scope, and two of them make the same product contradict itself in rial. Changes page vs GMV-change card: 470.6bn vs 479.7bn IRR for M156 on the default window (and the sessions LMDI attribution 410.5bn vs 420.4bn) — with a code comment at ChangesPage.tsx:26 explicitly claiming the two splits match. Ops platform KPIs vs the merchant table directly beneath them: 3.15× apart on the operator's own "who to chase" list. repeat_gap on short windows degenerates to "your entire week's revenue is recoverable" for 68% of the merchants it fires on. The realized-GMV cap is per-card, so M80's dashboard shows inbank_gap 908,040 + no_attempt_gap 908,040 against a total lifetime GMV of 908,040 — two cards each claiming 100% of everything sold. And /api/admin/merchants?sort=no_attempt is genuinely non-deterministic (5 distinct orderings across thread counts) with no sample-size floor, which is the exact ZB-120 defect class memory.md names as the #1 fix. A product whose headline claim is "every number is deterministic and traceable" cannot have two screens that answer the same question differently.

**Not lower because:** The engine itself is the best-verified analytics layer I have audited at this scale. Independent recomputation from the raw gzipped CSV reproduces the marts exactly — zero merchants deviate on sessions, verified or GMV. Grain discipline is real and load-bearing (7.3% count inflation at attempt level, correctly collapsed). Outcome taxonomy is internally airtight: 0 sessions verified/paid without attempted, 0 sessions with an OK attempt in a lost outcome, 0 paid_unverified without settled_at, 0 verified without verified_at. LMDI is exact to float precision, not approximately exact. NULL handling is disciplined throughout (nullif on every denominator; conv/rates return None rather than 0 on empty windows; inverted and out-of-range windows return honest zeros, not garbage). The GMV cap fires on every generator with zero per-card violations across 1,029 merchant-windows. Peer percentiles recompute by hand exactly. Every number in seven copilot answers matched an independent SQL recomputation that bypassed merchant_daily. And ZB-120's ntile tiebreaker is not cosmetic — I reproduced the original non-determinism by removing it (M250 gives 5 different results across thread counts) and confirmed it is gone with it.

**Independently verified strengths:**

- Marts reproduce exactly from the raw CSV. Independent DuckDB recomputation over data/other_challenge_data.csv.gz (2,213,289 rows, 2,062,839 distinct session_key, 343 merchants, 263,936 try_seq=0 rows) gives per-outcome counts AND amounts byte-identical to the marts — verified 1,025,655 / 5,165,464,690,799 IRR, abandoned_inbank 733,603 / 3,844,758,709,313, no_attempt 263,936, failed_bank 30,938, paid_unverified 8,706, reversed 1 — and a FULL JOIN of per-merchant (sessions, verified, GMV) between raw and merchant_daily returns ZERO mismatched rows.
- The six outcomes partition sessions exactly and are consistent with the timestamp columns. Counts sum to 2,062,839 with 0 NULL outcomes in both `sessions` and `merchant_daily`; 0 sessions are verified/paid without `attempted`; 0 sessions with an OK attempt land in a lost outcome; 0 sessions have settled_at outside (verified, paid_unverified); 0 paid_unverified lack settled_at; 0 verified lack verified_at; `recovered` is a strict subset of the first-fail pool in every merchant-day (0 violations). A 600-random-window funnel fuzz found 0 invariant breaks: outcomes always sum to stage[created], stages always monotonic, conv+all five loss rates always sum to 1 within 1e-9, every band/PSP rate inside [0,1].
- LMDI is exact, not approximately exact. Over 40 merchants × two 3-month periods, max |Σ contrib − ΔGMV| / |ΔGMV| = 2.3e-13, and the conversion-driver identity (Δconv = −Σ Δ of all five loss rates including reversed and paid_unverified) closes to 1.1e-16. Degenerate cases are guarded rather than fudged: `decomposable` goes false whenever any factor is ≤ 0.
- The ZB-120 ntile tiebreaker is real and load-bearing — I reproduced the original defect. With the shipped `ntile(5) OVER (ORDER BY amount, session_key)`, M156/M18/M250 return one identical (n_top, conv_top, conv_mid, avg_lost_amount) tuple across DuckDB PRAGMA threads = 1/2/4/8/16. Removing only `, session_key`, M250 returns 5 distinct results (conv_mid ranging 0.5607…0.5820, avg_lost 1,854,851…1,858,858) and M18 returns 3.
- Every number the copilot prints is the engine's number. Seven Persian questions on M156 produced sessions 55,940 / verified 30,508 / GMV 1,951,081,768,030 / conv 54.5% / median ticket 41,410,040 / customers 23,801 / first-fail pool 21,818 → 691 recovered (3.2%) / recovered GMV 40,641,116,040 / paid_unverified 912 @ 61,847,264,950 (3.2% of GMV) / no_attempt 6.1%, inbank 35.3%, failed 2.5% — all identical to an independent SQL recomputation run straight against sessions.parquet, bypassing merchant_daily. Peer percentiles also recompute by hand exactly (5/5 metrics, correct polarity inversion for lower-is-better rates), and analytics.overview's customer count matches analytics.customers' independently-derived count on 10 merchant-window pairs.

### Statistical methodology rigor — 56/100

*Lens `statistics` · confidence high*

The estimators themselves are correct and I verified the two hardest ones numerically: LMDI contributions close to ΔGMV at relative residual 2.75e-15 (M156) / 1.62e-15 (M250), and the conversion-driver identity to ~1e-17; the peer percentile is exact rank with correct tie polarity; the opportunity is a genuine counterfactual, not Σ failed amounts; and 117 of 131 peer-gap cards survive a one-sample z-test I applied. What fails is the gating, and it fails one-sidedly: every sample-size floor in the codebase protects the PEER side (MIN_PEERS=5, pool ≥500 sessions, peers HAVING ≥100) and not one protects the merchant's own denominator. Live consequence: /api/peers?m=M164 returns percentile 100 on all four metrics for a merchant with ONE session, with low_n=false, which the UI paints as four green "بهتر از ۱۰۰٪ از ۱۳ همتا" chips; 556 of 1687 published percentile rows come from merchants with <30 own sessions and 65% of those sit at the 0th or 100th percentile — the signature of pure noise. The gmv_change alert has no session gate at all and asserts a causal driver at confidence "high" on 2 sessions (M336, live); under a days-shuffled no-trend null its own ≥10% firing rule fires with median probability 0.76 (36/40 alerts ≥0.50) and the named driver reproduces at median 0.76, as low as 0.17 (M186, n=14, GMV 922k→1.3M rial). The advertised peer rule (scale ×¼–×4, ticket ×⅓–×3) is silently discarded for 281/343 merchants, whose median peer is 45× their size (p90 667×) drawn from a pool whose median conversion is 59% against 29% outside it. Quintile bands are equal-count over heavily tied prices: 84 published bands have lo==hi, and M106 reports 68.9/69.5/70.9/72.8% conversion for four bands that all contain exactly the amount 100,000.

**Not higher because:** The product's core statistical failure mode — presenting noise as a verdict — is not confined to a tail. The median merchant has 69 period sessions, and at that size the peer percentile, its green/red verdict chip and the "high confidence" change alert all produce claims that a permutation null reproduces most of the time. 187 of 343 merchants (55%) receive a published percentile on fewer than 100 own sessions with no caution of any kind, and 60 of 167 change alerts fire below 100 sessions, 11 of them below 15. A production fintech benchmark that tells a one-payment merchant it beats 100% of its peers, and a "why did sales change" diagnosis whose named cause survives 17% of bootstraps while labelled confidence "high", are shipping blockers, not tickets. Layered on that: the documented peer-matching rule is honoured for 12.5% of merchants, no family-wise error control exists across 9 generators × 5 benchmark metrics (90% of published percentile rows land on a coloured verdict), and ZB-047 (no significance test on the peer gap; the interval is a hardcoded constant) is verifiably still open — 21% of gap cards fire while the merchant is still inside the peer IQR.

**Not lower because:** The mathematics that exists is right and I checked it rather than trusting it. LMDI is exact to float precision on three real merchants and the conversion-driver identity closes including the easily-forgotten `reversed` term. The percentile is a genuine exact rank with no distributional assumption, and its tie handling is correct in both orientations. The opportunity estimator is a real counterfactual priced at the median ticket of the same lost outcome, and the product explicitly refuses the "lost revenue = Σ failed amounts" framing that would be the obvious cheat. Suppression, where it is aimed, is real and enforced, not decorative: MIN_PEERS removes the percentile keys entirely, the realized-GMV cap works (M136's inbank_gap is capped to 0 and honestly relabelled as a funnel alert rather than published as money), the PSP card restricts to try_seq=1 to avoid the retry-rail artefact and screens degenerate rails, and amount/PSP comparisons are within-merchant by construction so merchant-mix confounding is genuinely excluded. The gap card's own evidence states in Persian that its band is a conservative-to-optimistic scenario and explicitly not a statistical confidence interval — that is a level of methodological candour most products at this tier never reach.

**Independently verified strengths:**

- LMDI decomposition is exact, not merely asserted: I recomputed Σcontrib − ΔGMV for the Q1↔Q2 split and got relative residuals of 2.75e-15 (M156), 2.30e-13 (M31), 1.62e-15 (M250), and the conversion-driver identity Σconv_drivers − Δconv closes to 9.71e-17 / 8.67e-17 / 3.47e-17 including the `reversed` term that is easy to forget.
- The peer percentile is a true exact rank with no distributional assumption and correct tie polarity in both directions — `better = sum(1 for v in vals if (mine > v) == higher_better and mine != v)` excludes ties under either orientation, which I traced by hand and which tests/test_peers_benchmark.py locks at both extremes and mid-pack.
- The realized-GMV cap actually fires and relabels honestly rather than silently clamping: live /api/insights?m=M136 returns inbank_gap with impact_low=impact_high=0, capped=true, card_type flipped, and the label «در این دوره هیچ پرداخت موفقی ثبت نشده، پس برآورد ریالی قابل اتکا نیست» — the zero-GMV edge that _apply_gmv_cap's docstring says used to leak through uncapped is closed.
- Most peer-gap cards do survive a real test: applying a one-sample z-test of the merchant's rate against the peer median across all 343 merchants, 117 of 131 gap cards clear z=1.96, and the 14 that do not are all already marked confidence "low" — the 2pp floor plus the 100-session gate is doing more work than the flat constant suggests.
- The scenario-vs-CI distinction is stated where it matters most and in the machine-readable payload, not just prose: the live opportunity evidence for M136 carries caveats «بازه، سناریوی محافظه‌کارانه تا خوش‌بینانه است ... و بازه اطمینان آماری نیست» plus an explicit non-causality caveat «ادعای علیت ندارد: شکاف با همتایان هم‌زمانی است، نه اثبات علت», and the formula string reproduces the exact arithmetic (0.2318 − 0.1766) × 151 × [0.5 … 0.75 … 1.0] × 9,000,000.

### Security & privacy — 78/100

*Lens `security` · confidence high*

I attacked the live deployment and read the code paths behind every finding. The defensive engineering is genuinely strong: every injection surface is closed (m bound → 404 on `M156' OR '1'='1`, outcome whitelisted → 400, dates validated → 400, limit/sort int-cast/mapped → 422), HMAC tokens reject tampering (bad sig/forged payload/scope-mismatch all → 403) and expire, no PII/secret/source file is reachable, prompt injection is contained by routing + a multi-layer grounding guard, and security headers (HSTS, nosniff, frame-deny, no CORS) are present. Two real gaps keep it out of the top band: the entire operator/Control Center surface has NO real authentication on the public deploy — `POST /api/auth/session?scope=ops` mints a valid ops token to any anonymous caller (verified: 200 → all /api/admin/* return 200), behind a frontend OTP screen (Login.tsx) that validates nothing; and there is zero rate limiting — 15 rapid unauthenticated /api/copilot/polish calls all returned 200, each able to drive a real external LLM call (OPENROUTER_API_KEY is live). Mitigating both: the operator surface exposes only anonymized aggregate business metrics (no cards, no PII, no secrets), the models are :free ($0), and both postures are documented in memory.md as an intentional single-tenant demo. No wrong money, no data leak, no secret exposure was achievable.

**Not higher because:** The public operator surface has no real access control — an ops token is self-mintable by any anonymous caller and the phone+OTP login validates nothing, so all 343 merchants' platform-wide metrics sit behind decorative auth; and there is no rate limiting on any endpoint, giving an unauthenticated attacker unbounded external-LLM and serverless-invocation abuse. A real fintech would never ship either.

**Not lower because:** Every offensive vector I actually fired — SQL injection, path traversal, HMAC token forgery/tampering, scope escalation, prompt injection, secret/source-file exposure, and cross-merchant cache bleed — was properly closed and confirmed live. The one open surface is anonymized aggregates by documented design, and there is no wrong-money, PII, or secret exposure anywhere.

**Independently verified strengths:**

- SQL injection closed on every tested surface - verified live: m bound -> 404 on `M156' OR '1'='1`, outcome whitelisted -> 400, date validated -> 400, limit/sort int-cast/mapped -> 422. All f-string SQL (api.py:418, control.py:124, peers.py, analytics.py) uses constant fragments + DuckDB $-bound params via db.q().
- HMAC token integrity holds - verified live: tampered signature -> 403, forged payload with bogus sig -> 403, merchant-scope token against /api/admin -> 403. auth.verify uses hmac.compare_digest, enforces scope, expires at 12h, rejects future-dated iat.
- No source/secret file exposure - verified live: /.env.local, /app.py, /zarin/config.py, /.vercel/project.json all return the SPA index.html (lexical os.path.normpath containment to STATIC_DIR), not the files; .env.local (only a Vercel OIDC token) is untracked in git.
- Prompt injection contained - verified live: injection/PII questions route to out_of_scope refusal; a question carrying `http://evil.example` + 'account blocked' produced polish output with no link and no injected claim (grounding guard blocks forbidden links/emails/phones, ungrounded numbers, assertion/polarity markers, novelty). Evidence rows expose only session_key/amount/outcome/PSP - no payer_card_key.
- Admin responses never CDN-cached, no cross-merchant cache bleed - verified live: /api/admin/* returns Cache-Control: no-store, X-Vercel-Cache: BYPASS; cache keys include full path+query. Security headers (HSTS 2y preload, nosniff, frame-deny, Referrer-Policy) present on all responses; no permissive CORS (no ACAO header returned to Origin: evil.example).

### Reliability, error handling & observability — 61/100

*Lens `reliability` · confidence high*

I fired ~90 hostile probes at the live deployment (malformed/reversed/out-of-range dates, 501- and 10 000-char questions, NUL bytes, RLO overrides, emoji, Arabic yeh/kaf, ZWNJ, SQL and HTML payloads, limit=-5/0/999999999, unknown merchants, 7 path-traversal spellings, 6 token-tamper variants, a 24-way concurrent burst) and could not produce a single 500 from outside: the boundary validation is real and returns the offending value plus the expected format. The observability, however, is not trustworthy. Three of the product's own health numbers are quantifiably wrong in the optimistic direction: I reproduced an AI-Ops panel reporting fallback_rate 0.08 and p95 900 ms when the truth was 8 rejections in 10 LLM calls at 17 000 ms (10x and 19x understated), and the live deployment shows the same signature (fallback=14 with llm_requests=3). `Server-Timing: app;dur` — the header the product tells clients to trust — came back frozen at 302.9 ms with `Age: 121` on a CDN hit, and on 20 all-MISS concurrent requests read 61 ms p50 against 3103 ms wall. The request-telemetry ring silently reset from total=267 to total=7 mid-session with no indication, and 3000 anonymous `/api/<random>` requests produced 3001 endpoint rows and a 389 KB operator response while `attention` stayed empty at a 99.97% client-error rate. Worst: with `ZARIN_REQUIRE_AUTH=1` — the documented production mode and the recorded fix for ZB-001/ZB-030, both marked ✅ fixed — the response cache answers before the auth dependency, returning M18's 968 621 609 500 IRR GMV to an M156-scoped token under `Cache-Control: public, s-maxage=31536000`.

**Not higher because:** Three independent health metrics on the operator surface are measurably wrong in the flattering direction (AI fallback rate 10x low, AI p95 19x low, request p50 ~50x below user-observed latency), an anonymous caller can evict the telemetry ring and inflate the operator response 200x, and the tenancy fix recorded as ✅ fixed is bypassable by the process's own cache. A production fintech cannot ship an AI-health panel that reads 8% when the measured truth is 80%, nor an "attention" rule that stays silent while 99.97% of requests fail.

**Not lower because:** The trust boundary itself is genuinely well built: ~90 malformed/hostile inputs produced zero server errors, every rejection named the offending value and the expected form (`invalid date for f: 'banana' (expected YYYY-MM-DD)`, `question too long (max 500 chars)`, `Input should be less than or equal to 50`), path traversal in 7 spellings served the SPA index, all 6 token-tamper variants returned an identical non-leaking 403, and 24 concurrent uncached requests all returned 200 at 165 ms app p95. ZB-021's fix works — I forced a 500 and obs recorded error_rate 1.0 and raised a high-severity Persian attention item naming the path. 20 parallel cross-instance admin calls all succeeded, proving `ZARIN_SESSION_SECRET` is really set as the deploy invariant demands. None of the auth-cache damage is reachable on the deployed configuration today.

**Independently verified strengths:**

- ZB-021's fix is real, not just recorded as fixed: I monkeypatched analytics.overview to raise, and obs.summary() came back with error_rate 1.0 plus a correctly-worded high-severity attention item naming the path in Persian — the exception path genuinely reaches the telemetry ring through the cache middleware sitting between it and the route.
- Boundary validation survives sustained abuse: across ~90 hostile live probes (f=banana, f=2026-13-45, limit=-5/0/abc/999999999, outcome=DROP, m='' OR 1=1--, empty f1/t1, missing required params) I could not produce a single 5xx, and each rejection echoed the offending value with the expected form (`invalid date for f: 'banana' (expected YYYY-MM-DD)`, `Input should be less than or equal to 50`, `unknown outcome: 'DROP'`). The basic-ISO normalisation the team added is live: f1=20260101 and f1=2026-W01-1 both return 200 instead of the DuckDB 500 they used to.
- Unicode robustness is genuinely solid, which matters for a Persian RTL product: 15 payloads through /api/copilot — 10 KB of Persian, emoji, U+202E RLO override, an embedded NUL byte, raw newlines/tabs, Arabic yeh and kaf, Arabic-Indic digits, ZWNJ-shredded words, <script> and a SQL DROP string — all returned 200 with a correctly-routed or correctly-refused Persian answer, and the 500-char cap fired at exactly 501 (`{"detail":"question too long (max 500 chars)"}`).
- Token verification is textbook and I could not weaken it: no header, `Bearer zzz`, a truncated token, a signature with the last 4 bytes replaced, a merchant-scope token on an ops route, and the token without the `Bearer ` prefix all returned an identical `403 {"detail":"ops session required"}` with no oracle. auth.py uses hmac.compare_digest, rejects iat more than 300 s in the future, and enforces a 12 h max age.
- The deploy invariants hold under real conditions: 20 parallel /api/admin/platform calls with one token all returned 200 across multiple lambda instances, proving ZARIN_SESSION_SECRET is actually set (memory.md warns tokens "break at random" otherwise); a 24-way concurrent burst of uncached /api/overview queries returned 24/24 200s at app p95 165 ms; and 7 path-traversal spellings (/api/../../etc/passwd, /assets/../../zarin/config.py, /%c0%ae%c0%ae/) all served the SPA index rather than a file.

### Scalability & performance — 65/100

*Lens `scalability` · confidence high*

Server-side latency on the live deployment is real and honest: cold origin costs are 17.7 ms (/api/meta) to 191.8 ms (/api/customers), warm merchant endpoints sit at 25-50 ms, and I confirmed Server-Timing is not hiding queue time by running the control — 24 concurrent origin requests gave TTFB 1.8-3.8 s with app;dur 24.7-239.3 ms, but 24 concurrent requests for a static CDN asset that never touches the function gave the same 1.8-4.9 s spread, so the gap is my network to fra1, not the app. The design underneath is also sound where it matters: parquet_metadata() shows only 1 of 17 row groups (122,880 of 2,062,839 rows = 6.0%) is touched for a merchant filter, the per-thread-cursor DuckDB pattern took 192 concurrent requests with 0 errors, and peak working set is 154 MB over 63 MB of marts. What it does not survive is load or growth. Throughput peaks at 19.6 req/s at concurrency 4 and *falls* to 13.4 req/s at 32 while p99 goes 381 ms → 7,113 ms; on a 10x mart I built (20.6M sessions) peak throughput is 8.0 req/s, p99 19,836 ms, and peak RSS is 1,720 MB while DuckDB believes it has a 25 GiB memory_limit and points temp_directory at a relative '.tmp' that is read-only on Vercel. And the response cache returns bodies before FastAPI resolves Depends(_merchant_scope): with ZARIN_REQUIRE_AUTH=1, one authenticated fetch of M156 makes the identical 22,378-byte payload available to an anonymous caller and to a token scoped to a different merchant.

**Not higher because:** Three measured behaviours would block a real fintech deployment. (1) The system scales negatively: adding concurrency past 4 reduces throughput and multiplies p99 by 18.7x, because PRAGMA threads=4 on one shared DuckDB instance is fed by a 44-thread anyio pool with no admission control. (2) There is no memory back-pressure and no working spill path — DuckDB's memory_limit is left at its 25 GiB default and temp_directory at a relative path that is read-only on the target platform, so the 1,720 MB peak I measured at 10x becomes a silent OOM kill rather than a slow query. (3) The response cache short-circuits the merchant auth dependency, which I demonstrated end to end; the cache module's own docstring reasons about exactly this hazard for /api/admin/* and then misses it for the merchant routes. Add the architectural ceiling — 63 MB of parquet ships inside the function bundle, so 10x is a deploy failure, not a latency regression — and the ops telemetry that samples one lambda's ring buffer, and this is not "minor gaps".

**Not lower because:** Every performance claim I tried to break held up under an adversarial control. I assumed Server-Timing was under-reporting queue time and built the static-asset control to prove it; it did not — app;dur is an honest server-side number and the live warm p50 really is ~35 ms. I assumed the sorted-parquet claim in memory.md was decoration; parquet_metadata() shows 6.0% of rows scanned per merchant query, so it is buying real I/O reduction. I assumed the shared DuckDB connection would corrupt or error under load; the thread-local .cursor() pattern is the documented-safe one and survived 192 concurrent requests with 0 errors. I assumed the CDN's year-long s-maxage was pinning something mutable; I dumped every cacheable body looking for a generated_at, a counter, or a telemetry-derived field and found none. And the caching is a genuine fix rather than a slow query in a costume — the underlying uncached costs are 17-192 ms, so full coverage of 343 merchants across 9 endpoints is about five minutes of one-off CPU. Nothing here is fabricated or hand-waved, which is what separates this from the 40-59 band.

**Independently verified strengths:**

- Server-Timing is honest, and I tried to prove otherwise. Suspecting app;dur excluded queue time, I ran 24 concurrent origin requests (TTFB 1.802-3.781 s, app;dur 24.7-239.3 ms) and then the control — 24 concurrent requests for a static CDN asset that never touches the function — which produced the same 1.843-4.859 s spread. The gap is my own network to fra1, not hidden server time. Sequential warm merchant endpoints on the live deploy are genuinely 25-50 ms server-side.
- The sorted-parquet claim in memory.md pays off in real I/O. `SELECT row_group_id, stats_min, stats_max FROM parquet_metadata('data/marts/sessions.parquet') WHERE path_in_schema='merchant_key'` returns 17 row groups over 2,062,839 rows, of which exactly 1 (122,880 rows = 6.0%) has a [min,max] spanning M156; attempts.parquet is 1 of 16 (6.3%). Merchant queries touch ~6% of the file, not all of it.
- The DuckDB concurrency pattern is the correct one, not a lucky accident. zarin/db.py:38-51 gives each thread its own .cursor() off a single shared connection via threading.local, which is the documented-safe DuckDB idiom (sharing the connection object's execute() across threads is not). I drove 192 concurrent requests and 900+ total through it with 0 errors and no wrong results.
- Memory at the shipped scale is genuinely small because the marts stay as parquet views with projection/predicate pushdown rather than being materialised: PeakWorkingSet64 of the whole uvicorn process was 154 MB after a 32-concurrent load over 63 MB of marts, and a single /api/customers peaks at 89 MB in a fresh process. DuckDB's connect()+CREATE VIEW+schema guard costs 21 ms and count(*) over the full mart is 2 ms (parquet metadata, not a scan).
- Every CDN-cacheable body really is a pure function of path+query, which is what the year-long s-maxage depends on. I dumped /api/meta (keys: range, merchants, demo, notes) and /api/quality (outcomes, concentration, anomalies, rules_fa) specifically hunting for a generated_at, a request counter, or a telemetry-derived field that a 31536000 s edge TTL would freeze into a lie. There is none — the demo presets are computed from merchant_stats and the anomaly counts come from the pipeline sidecar.

### Product quality — 73/100

*Lens `product` · confidence high*

I ran the insight engine over all 343 merchants on two windows and probed the live deployment. Coverage is genuinely well-calibrated: 95.0% of platform GMV and 98.6% of sessions belong to a merchant that gets at least one money-denominated opportunity card, and among merchants with >=100 in-window sessions only 2.7% (2/73) get an empty feed on the 30-day view. The 22% of merchants who get nothing over 6 months have a median of 5 sessions — that is restraint, not emptiness. Peer benchmarking works for 343/343 merchants (median 13 peers), and card anatomy is real: the gmv_change card attributes the delta to traffic vs basket vs conversion and swaps the action accordingly, and each card routes to its supporting page. The last mile is thinner than it looks: only 4 of N cards ever render (8 opportunity cards across 15 merchants are unreachable, including two paid_unverified cards that are real collectable money), "فعلاً نه" is a dead control, and the "what to do" text is one of only 19 fixed strings across 538 cards — the action never carries the hour, amount-band or error-code detail the product already computes. The two surfaces are genuinely different products (API percentiles, AI grounding/cost, source adapters, a sortable follow-up table — none of it on the merchant side), but the Control Center's own assistant is 8 regexes with a catch-all, and its AI-Ops headline numbers dropped from total=128/hallucination_risk=23 to total=6/hallucination_risk=0 across two calls four minutes apart on the same token.

**Not higher because:** The product's own claim 7 ("answers the question that was asked, or refuses and says why") is falsified on both surfaces with reproducible evidence, at confidence=high rather than a clarify. The ops copilot has no refusal path in its code at all — every unmatched question returns the platform status line. On the merchant side the flagship merchant M31 (312,814 sessions, the largest in the dataset) is shown a top opportunity of exactly 114,479,770,001 IRR — 100.0% of its entire realized GMV — as a point estimate (low=mid=high), and demo merchant M265 shows the same shape. The GMV clamp fires on 22 cards with a median pre-clamp overshoot of 4.3x and a max of 100x, so the clamp is masking an estimator that is wrong by orders of magnitude, not trimming an edge. And the operator's single data-staleness signal reads 2018-10-20 for a 2026 dataset, badged green. A serious team would not ship the ops copilot or the M31 card in this state.

**Not lower because:** Coverage is measured, targeted and defensible rather than a demo-merchant illusion: I generated cards for every one of the 343 merchants, not just the 5 in meta.demo, and 143 merchants carrying 95% of GMV get a rial-denominated opportunity. Every card I read end-to-end contained the merchant's own numbers, a named counterparty where one exists (PSP-03 at 20.8% first-attempt success vs PSP-04 at 60.8% on the same merchant's traffic, with the top error codes), an effort chip, a sample size, and an evidence drawer. The empty state is two-branch and honest — a weak funnel gets "your conversion is X, that is not good news" plus a button to the funnel page, a healthy one gets "no gap found, that is a good sign". The click-through map (CARD_TARGET) and the done/skip checklist are real last-mile work, not decoration. And the Control Center is not a re-skin: it carries latency percentiles, AI fallback/grounding/cost, adapter status and a merchant follow-up table sortable by unverified money, all absent from the merchant surface.

**Independently verified strengths:**

- Coverage is calibrated, not lazy, and I measured it end to end rather than trusting the claim: generating cards for all 343 merchants (not the 5 in meta.demo) gives 95.0% of platform GMV and 98.6% of sessions belonging to a merchant with at least one rial-denominated opportunity card, from only 143/343 merchants. The 75 merchants who get nothing over 6 months have a median of 5 sessions and 4.32% of GMV between them.
- Restraint holds on the window that matters most. Re-running the engine on the last 30 days and gating on in-window volume: of 73 merchants with >=100 sessions in the window only 2 (2.7%) get an empty feed, and of the 40 with >=500 sessions, zero do. The naive "50% of merchants see nothing on 30 days" reading is an artifact of 178 merchants with almost no traffic.
- Peer benchmarking — the stated differentiator — actually works for the whole population, not just showcase merchants. Calling peers.peer_group for every merchant: 343/343 return sufficient=True, peer count min 5, p25 12, median 13, max 33. The 38 merchants with fewer than 8 peers get an explicit "مقایسه با N همتا — نامطمئن" chip in InsightCard.tsx, which I confirmed fires on M156 (n_peers=5) in the live /api/insights payload.
- The change card is genuinely diagnostic, not a delta restatement. Reading the 538 generated cards, gmv_change attributes the move to traffic vs basket size vs conversion and swaps the action accordingly — M43 got "افت ۷۲٪ … کاهش ترافیک عامل اصلی بوده — کانال‌های جذب را بررسی کنید" while M18 got "رشد ۲۵٪ … رشد از بزرگ‌تر شدن سبد خرید آمده — همین ترکیب محصول/قیمت را حفظ کنید". The live copilot reproduces the same decomposition with signed contributions per factor.
- The two surfaces are genuinely different products, verified by pulling all five admin payloads with an ops token: /api/admin/performance carries per-endpoint p50/p95/p99 and an attention list, /api/admin/ai-ops carries grounded_rate, fallback_rate, token and cost totals and per-intent counts, /api/admin/sources carries adapter status with a truth-source flag, /api/admin/platform carries category distribution and top5_share concentration. None of these fields appears on any merchant endpoint, and the merchant surface's peer/funnel/customer analytics appear on none of the admin ones.

### Business viability & value proposition — 66/100

*Lens `business` · confidence high*

I recomputed the headline value number straight from the parquet and it is exact: platform paid_unverified = 8,706 sessions / 116,550,558,678 IRR, byte-identical to /api/admin/platform. That is 2.26% of the 5,165bn IRR verified GMV — real, traceable, and the kind of number a PSP merchant-success team can actually call on. The moat is also real: category peer medians and cross-merchant PSP-rail comparisons need data only the PSP holds, so this is not repackaged BI. But the commercial picture underneath is much thinner than the demo. 53.1% of that flagship 116.5bn sits in one merchant (M156, the default demo merchant); the median affected merchant has 29.8M IRR (~$30 over six months); and 18.5% of it (21.5bn across 23 merchants) sits where unverified money exceeds the merchant's entire realized GMV — M30 has ZERO verified revenue ever and still gets a confidence:high card for 3.98bn labelled "real amount, not an estimate" with a diagnosis that is provably wrong. Insight yield collapses at the cadence the product claims for itself: running zarin.insights.generate over all 343 merchants gives 22% empty feeds on the 6-month window, 50% at 30 days, and 79% at 7 days. And the whole merchant API is unauthenticated on the live deployment — 343 merchants' GMV, customers and row-level sessions readable by anyone, with Cache-Control: public.

**Not higher because:** A live product that serves 343 merchants' revenue and per-session rows to anonymous callers cannot enter a PSP pilot at all — and the ops surface IS gated, so this is an omission, not an inability. Beyond that, the two things a buyer underwrites both fail verification: (a) the engagement story — PRODUCT.md says the merchant opens it weekly, but 270/343 merchants (79%) get an empty feed on a 7-day window and 172/343 (50%) at 30 days; (b) the money story — the flagship 147.3bn IRR opportunity on M156 is inflated ~44% by a peer baseline that includes M265, whose 94.4% no-attempt rate the project's own docs call a broken integration (peer median in-bank abandon 26.5% with it, 29.2% without; gap 8.76pp vs 6.07pp), and 18.5% of the headline unverified money carries a confidently-wrong diagnosis. There is also no metric anywhere — Control Center included — that tells the buyer what Zarbin is worth: it tracks latency, fallback rate and grounded rate, and feedback total is 0.

**Not lower because:** The value numbers survive independent recomputation to the rial, not just to the headline: my own DuckDB query over data/marts/sessions.parquet produced 8,706 / 116,550,558,678 and /api/admin/platform returned exactly those digits. The differentiation is genuine and PSP-exclusive — no merchant-side tool can compute a category peer median or compare PSP-03 against PSP-04 on the same merchant's first attempts. The refusal discipline is implemented, not just documented: _apply_gmv_cap clamps estimates to realized GMV and converts zero-GMV merchants to alerts, gaps under 2pp emit no card, n_peers<8 forces low confidence, and both empty states are honest. The copilot answered «چرا فروشم کم شد؟» by correcting the premise ("your sales did not decline") and giving the LMDI decomposition — that is advisory behaviour, not a chart. And for the top 50 merchants by GMV, which carry 72% of platform volume, the engine produces cards for 94-96% of them at 30-day and 6-month cadence. That is a shippable operator tool for the segment that matters.

**Independently verified strengths:**

- The headline commercial number reproduces exactly. I computed platform paid_unverified independently from data/marts/sessions.parquet with DuckDB — 8,706 sessions, 116,550,558,678 IRR, settled_at non-null on 100% of them and verified_at null on 100% — and /api/admin/platform returned {"paid_unverified":8706,"paid_unverified_amount":116550558678}, the identical digits. The traceability claim holds for the number the business case rests on.
- The moat is genuinely PSP-exclusive, not repackaged BI. /api/peers?m=M156 returns an exact-rank percentile against a named, rule-stated category cohort, and _card_psp_friction compares first attempts only across rails on the same merchant (PSP-03 20.8% vs PSP-04 60.8% on M156). Neither a merchant nor any merchant-side analytics tool can compute a cross-merchant peer median or a rail comparison — that data exists only inside the PSP.
- The refusal discipline is implemented, not just claimed. I read _apply_gmv_cap: it clamps every rial estimate to realized GMV and converts cards on zero-GMV merchants into funnel alerts with impact 0 rather than publishing money; _gap_card returns None below a 2pp gap and forces confidence='low' under 8 peers. Running generate() across all 343 merchants, 201 of them get no rial-denominated opportunity at all — the engine really does decline to invent numbers on thin evidence.
- The copilot exhibits advisory behaviour, not lookup. Asked «چرا فروشم کم شد؟» (a loaded premise) against M156 on the live deployment, it answered «فروش شما در این بازه کم نشده است» and then gave the LMDI decomposition of a +479.7bn IRR increase. Correcting the question is the thing a dashboard structurally cannot do.
- For the segment that would actually be sold, coverage is strong. Across the top 50 merchants by GMV — which carry 72% of platform volume per the concentration figure I confirmed at top5_share=0.724 — generate() produces at least one card for 47/50 on the 6-month window and 48/50 on the last 30 days, versus 268/343 and 171/343 across the whole base.

### UX & usability (non-technical merchant) — 66/100

*Lens `ux` · confidence high*

I drove the live deployment in a real Chrome (desktop 1440 and mobile 390, full merchant login flow, all 7 pages, 3 merchants) and fetched ~50 copilot/polish answers. The Persian copy itself is the best thing here: the 21-entry TIPS glossary is written for a shop owner, not an analyst ("یک سوپرمارکت را نباید با طلافروشی سنجید"), insight cards run مشاهده→تشخیص→اقدام, caveats sit on the surface rather than in a footnote, and every page has real empty-state copy. But the merchant-facing surface has defects that actively mislead. Asked "دیروز چقدر فروختم", the deployed UI renders "دیروز ۵۵٬۹۴۰ جلسه پرداخت ثبت شد … فروش موفق ۱٫۹۵ هزار میلیارد ریال" tagged اطمینان بالا — the LLM polish pass relabels the 6-month total as "yesterday" and the grounding guard passes it (8 of 15 polish calls fabricated a time frame; grounded=true, quality_flags=[]). 59 of 343 merchants get an "اطمینان بالا" growth verdict plus prescriptive advice computed from 2–15 transactions (M336: 2 sessions in 6 months → "رشد ۱۸٪ … همین ترکیب محصول/قیمت را حفظ کنید"), and the same merchant is told "بهتر از ۱۰۰٪ از ۱۱ همتا" on four metrics with no caution chip. On a phone, tapping any glossary term never opens the tooltip (.tip-pop count 0 after tap and after click; 1 only after hover), so the whole plain-language layer is dead for the target user. Headline money figures on the landing KPI strip and on every insight card print "۶۱٫۸ میلیارد" with no ریال — a 10x ambiguity in a country that reads amounts in toman.

**Not higher because:** Three independent ways a non-technical merchant is actively misled or blocked, all reproduced live: (1) the chat surface will state a 6-month total as "yesterday's sales" with اطمینان بالا, reproducibly (8/15 polish calls) — the product's own composer note says «اعداد هرگز توسط هوش مصنوعی ساخته نمی‌شوند» while the AI is inventing the time frame those numbers belong to; (2) 17% of merchants (59/343) receive high-confidence business advice derived from single-digit transaction counts, and a 2-transaction merchant is ranked 100th percentile against 11 peers with no warning; (3) the entire glossary — the mechanism that makes this product understandable to a shop owner — cannot be opened on a touch device. Add a refusal class (`clarify`) that still renders as a confident answer with its three suggested questions silently dropped, and two pages one click apart quoting different growth numbers for the same period, and this is not "minor gaps".

**Not lower because:** Nothing in the deterministic layer produces a wrong number, and the craft is well above MVP. The Persian is genuinely plain and merchant-shaped rather than translated analyst-speak; caveats are surfaced (the card-scope limitation is a banner at the top of the customers page, the cohort legend explains its own colour scale is relative not 0–100%, the opportunity label says outright "نه بازه اطمینان آماری"). Refusals are non-judgemental and offer a capability list, and "چرا فروشم کم شده؟" corrects the false premise instead of playing along. Empty and low-data states all have specific copy rather than blanks. Mobile is clean: scrollWidth == clientWidth == 390 on all seven pages, the fixed 56px bottom nav is cleared by 88px of main padding, zero console errors. The evidence drawer is a real traceability affordance with a working focus trap, Escape, scroll lock and focus restore, and it opens correctly by tap.

**Independently verified strengths:**

- The plain-language glossary is real content, not decoration: I read all 21 TIPS entries and they are written for a shop owner — conv is «از هر ۱۰۰ مشتری که به صفحه پرداخت رسیدند، چند نفر پرداختشان کامل شد», peers justifies itself with «یک سوپرمارکت را نباید با طلافروشی سنجید», and each has the same یعنی چه/چرا مهم است/چطور تفسیر کنم structure. ZB-031 (tooltips not wired into any analytics page) is genuinely fixed — I can see the dashed <Term> affordances rendered on Overview, Funnel, Peers, Customers and Changes in the live screenshots.
- Mobile layout is clean, which I measured rather than eyeballed: document.scrollWidth === clientWidth === 390 on home, funnel, changes, peers, customers and quality; the bottom nav is a single position:fixed 56.4px bar at z-index 25 with an opaque white background and main carries padding-bottom:88px, so nothing is occluded; zero console errors or pageerrors across the whole flow.
- Empty and low-data states are specific, not blank. M11 (1 session, 0 verified): copilot returns «در این بازه مشتری پرداخت موفقی ثبت نشده است» and «در این بازه داده کافی برای تجزیه تغییر فروش وجود ندارد (یکی از دوره‌ها فروش موفق ثبت‌شده ندارد)»; the zero-card overview splits into a genuine all-clear versus «فرصت مشخصی برای پیشنهاد پیدا نشد، اما مسیر پرداخت شما ضعیف است» plus a button into the funnel (the ZB-003 fix, working); suppressed peer rows say «داده کافی برای این مقایسه وجود ندارد» instead of a blank.
- The evidence drawer is a working traceability affordance on touch, not just on desktop. I tapped an insight card's «این عدد از کجا آمد؟» at 390px and got the full panel: definition, formula, Jalali period ۱۱ دی ۱۴۰۴ تا ۹ تیر ۱۴۰۵, grain, sample size ۹۱۲, the executed SQL, both caveats, and a «نمایش نمونه جلسه‌های منبع» button. The code backing it (EvidenceDrawer.tsx:29-58) implements role=dialog aria-modal, a Tab focus trap that pulls focus back when it escapes, Escape-to-close, body scroll lock and focus restoration to the opener.
- Refusals and false-premise handling are well written. Forecast («ماه بعد چقدر میفروشم؟»), PII («شماره تلفن مشتری‌ها»), prompt injection and greetings all return a calm «این پرسش خارج از چیزی است که از داده پرداخت‌های شما قابل محاسبه است» plus an explicit list of what the product can answer, with no scolding and no leakage. And «چرا فروشم کم شده؟» does not play along with the premise — it answers «فروش شما در این بازه کم نشده است» and then decomposes the growth, which is exactly the right behaviour for a merchant working from a wrong assumption.

### Visual & interaction design, Persian/RTL craft — 68/100

*Lens `design` · confidence high*

I probed the live deployment with a real Chrome (Playwright, locale fa-IR), captured 18 screenshots across all 12 pages in both workspaces, and measured per-glyph x-positions rather than trusting OCR. The token system is genuinely disciplined: 46 CSS custom properties, exactly one used-but-undefined (--line-strong) and one defined-but-unused (--fs-2xl); zero physical directional properties in theme.css outside deliberately `direction: ltr` code/SQL/path blocks; zero horizontal overflow at 412px. A composited-alpha WCAG probe over 12 pages found 0 AA text failures (worst 4.64:1 against a 4.5 requirement), so the previous panel's ZB-035 fixes hold. The Persian numeral system is CLDR-correct and verified byte-identical across the Python/TypeScript boundary — U+066C group, U+066B decimal, U+066A percent — with 0/4000 mismatches on fuzzed money. But two Persian-order defects sit on the primary screens: all 16 axis labels on the flagship trend chart render number-left-of-word (visually "میلیارد ۳۰", "تیر ۹") because the chart wrapper is `dir="ltr"`, and every signed delta paints its sign to the *right* of the digits — the opposite of what the product's own `Intl` output does, because `pp()` and the Waterfall hand-roll the sign without the LRM that Intl emits. The Waterfall's zero baseline is painted with an undefined token so it computes to `rgba(0,0,0,0)`. And `data-workspace` is emitted on `.app` but no CSS rule anywhere consumes it, so Control Center is chromatically identical to the merchant workspace despite the login screen teaching yellow=merchant / blue=ops.

**Not higher because:** The single most-viewed chart in the product has every axis tick reading backwards in Persian, and signed numbers across three surfaces put the sign on the wrong side of the digits — visible typographic errors on primary screens, not edge cases, in a product whose whole pitch is Persian-first trustworthy numbers. On top of that a diverging bar chart ships with no visible zero baseline, the operator surface is visually indistinguishable from the merchant surface despite the attribute being wired for it, and Latin digits leak into three Persian-numeral surfaces (the 24-hour heat strip, cohort headers, the AI cost tile) while the `HOURS_FA` helper written for exactly that job is never imported. A strong-band build would not have any of these on the happy path.

**Not lower because:** Nothing here blocks production and none of it is sloppiness at scale. Logical properties (`border-inline-end`, `margin-inline-start`, `text-align: start`, `inset-inline`) are used consistently and the only physical ones are inside intentionally LTR SQL/formula/path/phone contexts. I measured zero WCAG AA text failures across 12 pages including the alpha-composited cohort grid. The numeral and separator system is CLDR-correct on both sides of the language boundary and I fuzzed it (4000 money values, 0 mismatches; U+060C never used as a group separator anywhere in the rendered DOM). Every bar chart I measured is proportionally honest — the ops category distribution's pixel ratios (1.0 / 0.764 / 0.423 / 0.112 / 0.082) track its value ratios (1.0 / 0.771 / 0.404 / 0.109 / 0.084). Reduced-motion, coarse-pointer 44px targets, safe-area insets, skeletons and empty states are all present. One undefined variable out of 46 is tighter token hygiene than most shipped design systems.

**Independently verified strengths:**

- Zero WCAG AA text-contrast failures across all 12 pages in both workspaces. I ran a composited-background probe (walks the ancestor chain, alpha-blends each layer, handles SVG `fill`) on merchant overview/funnel/peers/customers/changes/quality/copilot and ops overview/performance/ai/sources/copilot: 26-48 distinct colour/size combos per page, 0 below threshold, worst ratio 4.64:1 against a 4.5 requirement (a cohort-grid cell on /#/customers). The ZB-035 remediation genuinely holds, including the alpha-ramp cells the previous panel flagged.
- The Persian numeral system is CLDR-correct and byte-identical across the Python/TypeScript boundary. Node's `Intl.NumberFormat('fa-IR')` emits group U+066C and decimal U+066B (`6f1 66c 6f2 6f3 6f4 66c 6f5 6f6 6f7` for 1234567); zarin/fa.py's translate table maps to the same two code points; a 4000-value money fuzz and a 4000-value integer fuzz produced 0 mismatches. On the live rendered DOM I found 0 occurrences of U+060C or ASCII comma used as a group separator and 0 ASCII decimal points between Persian digits, and the percent sign is the Arabic U+066A on every page — matching `Intl.NumberFormat('fa-IR',{style:'percent'})` exactly.
- Logical-property discipline is real, not claimed. `grep -n -E "(margin|padding|border)-(left|right)|^(left|right):|text-align: *(left|right)|float"` over theme.css returns exactly two hits, both inside blocks that deliberately set `direction: ltr` (.formula-box and .sqlbox for SQL/formula display). The physical props in TSX inline styles are all in the same category: SQL text, API path cells, the phone-number input, and absolutely-positioned marks inside explicit `dir="ltr"` chart tracks.
- Token hygiene is tighter than most shipped systems: I diffed all 46 `--*` definitions in theme.css against every `var(--…)` reference in .ts/.tsx/.css. Exactly one is used-but-undefined (--line-strong, finding above) and exactly one is defined-but-unused (--fs-2xl). Nothing else drifts.
- The bar charts are proportionally honest, measured rather than assumed. On the ops category-distribution chart, pixel bar lengths ratio to 1.0 / 0.764 / 0.423 / 0.112 / 0.082 against value ratios 1.0 / 0.771 / 0.404 / 0.109 / 0.084. On /#/peers the percentile markers match their chips (chip «بهتر از ۶۰٪» → marker 39.8% from the left; «بهتر از ۲۰٪» → 79.8%). At 412px (Pixel 7, pointer:coarse) `document.scrollWidth === clientWidth === 412`, zero elements exceed the viewport, and all seven bottom-nav targets measure 59x55px.

### Accessibility & inclusive design — 64/100

*Lens `accessibility` · confidence high*

I computed every CSS token pair by hand and drove the live deployment through headless Chrome/CDP with axe-core 4.x. The colour system is genuinely solid: all 16 ink×surface text pairs pass 1.4.3 (worst 4.68:1, --ink-4 on --surface-3) and all 10 semantic chip pairs pass (worst --bad on --bad-bg at 4.68:1) — ZB-035 held. The evidence drawer's focus trap is real: 30 Tab + 8 Shift-Tab presses never escaped it, Escape and backdrop click both restored focus to the exact opener node, and body scroll locked/unlocked correctly. prefers-reduced-motion actually zeroes durations (measured animation-duration: 1e-05s), the page reflows cleanly at 320 CSS px (scrollWidth 320 == clientWidth 320, zero elements wider than viewport, axe 0 violations), and the four authenticated Control Center pages return zero axe WCAG A/AA violations. But six A/AA criteria fail on the merchant surface. There is no skip link at all: the first focusable element is the first sidebar button and content begins at tab stop 13, on every page. The 21 metric tooltips fail 1.4.13 in both directions — I measured the popover staying open after Escape when opened by hover (activeElement was BODY, so the trigger's key handler never runs) and closing the instant the pointer moves onto it (pointer-events: none, 10px gap). And the copilot — the product's headline surface — has zero live regions during a measured 9,286 ms wait, then inserts its one aria-live node together with its content.

**Not higher because:** Six distinct WCAG 2.2 A/AA criteria fail on the primary merchant surface, and they land on exactly the two features the product sells: the metric glossary tooltips (1.4.13, both Dismissible and Hoverable) and the copilot answer stream (4.1.3 — 9.3 seconds of total silence, then an announcement that spec-wise will not fire). A blind merchant gets the string "روند روزانه فروش موفق" and nothing else from the trend chart (role="img" over 142 characters of hidden axis text, no data table, no aria-describedby). Every keyboard user pays 12 redundant chrome tab stops per entry with no bypass, and five of seven merchant pages plus the entire login screen have no h1 to jump to instead. Four of these (ZB-090..094) were already logged by the previous panel as MEDIUM/unverified and are all still open — I verified each is present in the live build. That is not "production-grade for a real fintech".

**Not lower because:** Nothing here blocks use outright, and the defects are omissions inside a coherent system rather than evidence of a system built without accessibility. The token palette is measurably correct across all 26 text pairs I computed; the focus indicator (2px solid #2333e0, 8.01:1 on white, offset 2px) is applied consistently and I saw it on 22 consecutive tab stops; the modal focus trap and focus restoration are implemented properly, which most MVPs get wrong; reduced motion and 320px reflow genuinely work rather than being claimed; `<html lang="fa" dir="rtl">` is correct with no maximum-scale lock; the voice input is purely additive over a fully working text path (typed query + Enter produced an answer with the mic absent from the flow); and the authenticated ops surface is axe-clean on all four pages. The previous panel's two fixed a11y items (ZB-035 contrast, ZB-036 composer focus ring) both verified as genuinely fixed.

**Independently verified strengths:**

- Evidence drawer focus trap is correct, not decorative: I opened it from a tagged opener and pressed Tab 30 times then Shift-Tab 8 times — focus never left `.drawer` ("NONE — trapped"). Escape closed it and `document.activeElement.id === 'OPENER'` was true; clicking the backdrop did the same. `document.body.style.overflow` was "hidden" while open and "" after. Caveat: the page behind is neither `inert` nor `aria-hidden` (39 focusables remain), so `aria-modal="true"` is doing that half of the work.
- Colour tokens genuinely pass 1.4.3 — I recomputed all of them rather than trusting the ZB-035 comment. All 16 ink×surface pairs pass (worst --ink-4 on --surface-3 at 4.68:1; --ink-3 on --bg 5.70:1), and all 10 semantic pairs pass (--good/--good-bg 5.55:1, --bad/--bad-bg 4.68:1, --warn/--warn-bg 5.12:1, --blue on #eef0ff 7.07:1, #8a6d00 on --brand-soft 4.61:1). axe reported zero color-contrast violations on all 7 merchant and 4 ops pages. Recharts' default #666 axis ticks measure 5.74:1 on white — also passing.
- prefers-reduced-motion is honoured for real, not just declared: with `Emulation.setEmulatedMedia` forcing `prefers-reduced-motion: reduce`, the computed style on `.funnel-fill` came back `animationDuration: "1e-05s", transitionDuration: "1e-05s"` on every element I sampled.
- 1.4.10 Reflow is clean at 320 CSS px: `{scrollW: 320, clientW: 320, hOverflow: false}`, zero elements wider than 321px anywhere in the body, axe returns `[]` for wcag2a/2aa/21aa/22aa at that width, the 248px sidebar is swapped for a bottom nav whose items measure 46×55 (comfortably over 2.5.8's 24×24), and `<meta viewport>` sets no maximum-scale or user-scalable lock.
- The voice input is purely additive over a working text path, and the focus indicator is consistent. Keyboard-only: typed into `.composer input` (aria-label «پرسش از زرین‌بین»), the send button enabled, Enter submitted, and a deterministic answer rendered — the mic is gated on `SpeechRecognition` support and carries correct `aria-pressed` + a label that toggles to «توقف ضبط صدا». Every real `<button>` I focused across 22 tab stops showed `outline: 2px solid rgb(35, 51, 224)` at 8.01:1 on white, and ZB-036's composer fix verified as present (`:focus-within` gives border #2333e0 plus a 2px ring). The four authenticated Control Center pages each have exactly one h1 and return zero axe WCAG violations.

### AI quality, grounding & AI-Ops — 62/100

*Lens `ai-grounding` · confidence high*

The LLM-off-the-answer-path design is a real engineering decision, not an excuse: I verified /api/copilot hardcodes use_llm=False, that with OPENROUTER_API_KEY="" default_provider() returns None and use_llm=True still yields source='deterministic', and that the client only swaps wording after rendering. The guard's designed-for classes all genuinely hold under attack — rial→toman, percent→rial, 1.95 هزار میلیارد→1.95 میلیارد, negation-drop, invented causality and three live prompt-injection payloads were all rejected. But the numeric layer is a multiset-membership test (gateway.py:196-220): it checks each LLM number exists somewhere in the deterministic text, never that it stays bound to the same metric. That is not theoretical — in a clean serial batch of 8 live calls to https://zarbin-nine.vercel.app/api/copilot/polish, 3 of the 7 accepted answers restated the engine's MEDIAN (41.4M rial) as the MEAN/«میانگین»/«متوسط», each shipped as grounded:true, source:"llm", quality_flags:[]. The true mean is 63,953,119 rial (DuckDB), so the merchant is shown a 35%-wrong average ticket labelled engine-traceable. In the lab the same hole rebinds 61.8B «مبلغ واقعی ... برآورد نیست» into 147.3B, swaps verified↔sessions, and turns percentile 60 into percentile 5. An empty completion is also "grounded" and Copilot.tsx:81 swaps it in. The AI-Ops surface that should catch this dilutes fallback_rate over deterministic requests (18% reported vs 40% real) and publishes routing_unsafe_rate=0.0 from the dev set while its own holdout says 0.05 and retrieval.py's docstring calls that laundering.

**Not higher because:** Product claim 1 ("every number is computed by the deterministic engine", gateway.py:6 "the numbers a merchant sees are therefore always the engine's, never the model's"; api.py:319 "a swap can only ever change wording") is false on the live deployment today, reproducibly, at ~40% of accepted polish responses for the gmv intent — a median published as a mean is wrong money on a merchant surface. The guard is structurally permutation-blind and every one of the 14 entries in tests/test_grounding_calibration.py::ATTACKS is an *append* attack, so the calibration set cannot see the class that is actually failing. The operator surface built to make AI quality visible reports the two numbers that would expose it (fallback_rate, routing_unsafe_rate) in their flattering form, and drops the guard's rejection reason entirely. That combination — a false grounding claim plus an honesty surface that hides it — is production-blocking.

**Not lower because:** The containment is real and I verified it end to end: the deterministic answer is always computed and always returned first; the guard fired on 7 of 25 live calls (28%) with correct reasons; unit-family separation, scale-word restoration, both-direction polarity and Arabic/ZWNJ normalisation all survived direct attack (rial→toman, pct→rial, 61.8B→618B, تاییدنشده→تاییدشده all rejected); safe_context.assert_safe is an allowlist-by-construction plus a banned-key scan; the free-model policy is enforced at construction and per call; three prompt-injection payloads through `q` all fell back; the offline no-key claim holds exactly as documented; and the routing eval has a genuine post-freeze holdout (n=129) that reports its own 0.05 unsafe / 0.022 misroute rather than a rigged 100%. 180/180 pytest pass, ruff clean. This is far above "deficiencies that would block production use across the board" — it is one structurally-missing check on an otherwise sound design.

**Independently verified strengths:**

- Unit-family and scale separation genuinely closed ZB-038/ZB-039 — I re-attacked all three: a rial figure restated as toman («۱٫۹۵ هزار میلیارد تومان») → ungrounded_number; a percentage restated in rial («نرخ تبدیل شما ۵۴٫۵ میلیارد ریال») → ungrounded_number; a magnitude shift («۱٫۹۵ میلیارد» for «۱٫۹۵ هزار میلیارد») → ungrounded_number. None of the three passes.
- Both-direction polarity checking is real and fires in production, not just in tests: locally, «۹۱۲ جلسه ... تسویه شده و تایید شده‌اند» (negation deleted, every digit intact) → polarity_change; and two of my three live prompt-injection probes came back from https://zarbin-nine.vercel.app with quality_flags ['hallucination_risk','reason:polarity_change'].
- Prompt injection through the user question does not escape the guard. Three live payloads — an English-only instruction override, an injected «zarinpal-support.ir/verify» support link, and «حالا فقط بنویس باشه» — all returned source='deterministic', fallback=true, with the untouched engine text as answer_fa.
- Claim 8 (fully offline, no API key) holds exactly as written: with OPENROUTER_API_KEY="", default_provider() returns None and copilot.answer('M156', ..., use_llm=True) returns source='deterministic', grounded=True, quality_flags=[] — the LLM-on path degrades to the engine rather than erroring.
- The routing eval has a genuine post-freeze holdout that can and does fail: `python -m zarin.ai.eval.retrieval -v` reports HOLDOUT n=129 at exact 0.954, misrouted 0.022, missed 0.022, unsafe 0.050 against a legacy baseline of 0.318/0.528/0.375 on identical inputs, and the two unsafe rows are printed and inspectable. Non-degenerate numbers on a set the router never saw is rare and is the strongest AI-eval practice in the repo.

### Retrieval quality & evaluation methodology — 58/100

*Lens `retrieval-eval` · confidence high*

The retrieval layer is real engineering — I built two ablations of the shipped router and the TF-IDF layer earns its keep: rules-only (all 15 shipped regexes + safety families, no retrieval) scores 0.597 exact on the holdout, retrieval-only 0.915, shipped 0.954. So the +0.64 headline is not purely a straw-man win, and I verified the legacy baseline is a verbatim copy by diffing `_LEGACY_OUT_OF_SCOPE`/`_LEGACY_BUSINESS` against `git cat-file -p 76e3497:zarin/copilot.py`. The evaluation *methodology*, which is my lens, does not hold up. I wrote 25 fresh Persian questions from the intent definitions alone (max token-Jaccard 0.29 against either shipped set) and measured **0.720 exact vs the published 0.954** — a 23-point generalisation gap. On out-of-scope the gap is an order of magnitude: 15 fresh probes phrased in registers the literal keyword families do not enumerate gave **9/15 = 0.600 unsafe against a published 0.050**, and the live deployment answers «سود خالص من بعد از کسر بهای تمام‌شده چقدره؟» (net profit after COGS) with retry-recovery numbers at `confidence: high, grounded: true, fallback: false`. The calibration is also leaky: `pipeline/calibrate_nlu.py` holds out each bank example but not the anchors derived from it — 132 of 170 anchors appear verbatim inside a same-intent bank example, and the 0.920 LOO figure that justifies ACCEPT=0.13 falls to **0.702** when the matching anchors are held out too. Three provenance claims asserted in shipped code are false as written: "zero overlap" (there is one normalised exact duplicate plus J=0.71/0.68 near-dups), "nothing in the router has been changed in response to any question in this file", and "character 3-grams survive the missing ZWNJ" (they do not — holdout drops 0.954→0.930, my set 18/25→16/25).

**Not higher because:** The published generalisation number is 23 points optimistic against questions written outside the project (0.720 vs 0.954) and the published safety number is off by 12x (0.600 vs 0.050 unsafe on fresh probes). The one metric shipped to operators — `/api/admin/ai-eval` `routing_unsafe_rate: 0.0` — is computed from the dev set the repo's own docstring calls "a floor, not the headline", and reads zero for a failure mode I reproduced live on a public endpoint. The "leave-one-out" calibration leaks 22 points through un-held-out anchors, so the constants are not calibrated as cleanly as claimed. Three statements in `zarin/nlu.py`, `retrieval_holdout_cases.py` and `retrieval.py` directly contradict the honesty note in `docs/RETRIEVAL.md` §4, and only the dev set has a CI floor, so a holdout regression is invisible.

**Not lower because:** The retrieval design is genuinely load-bearing, not decoration: my rules-only ablation (0.597) vs shipped (0.954) on the holdout, and 0.280 vs 0.720 on my own set, prove the TF-IDF layer contributes ~+35 points over the same author's hand-written regexes. The baseline is honest — I confirmed `_LEGACY_OUT_OF_SCOPE` and `_LEGACY_BUSINESS` are byte-identical to commit 76e3497, and counting the legacy `fallback` branch as an answer rather than a refusal is the conservative choice. `current_route = copilot.route_intent` means the eval scores the deployed function with no possibility of drift. The five-outcome breakdown (separating `misrouted` from `missed` from `unsafe`) is better methodology than the single accuracy most projects publish. Arabic ك/ي folding works exactly as claimed (0/25 routes changed under full substitution), the similarity score is informative on unseen data (correct median 0.248 vs wrong 0.155 on my set), and §4/§5 of RETRIEVAL.md volunteer several of these problems unprompted.

**Independently verified strengths:**

- The retrieval layer genuinely earns its keep, not a straw-man win. I built two ablations of the shipped router in pipeline/_panel/ablate.py: rules-only (safety families + all 15 ordered regexes, no TF-IDF) scores exact 0.683 dev / 0.597 holdout / 0.280 on my own set; retrieval-only scores 0.917 / 0.915 / 0.720; shipped 0.958 / 0.954 / 0.720. The TF-IDF layer adds ~+36 points over a fair regex baseline built from the project's own rules — far more than the 8-regex legacy baseline flatters it by.
- The legacy baseline is a faithful copy, verified against history. `git cat-file -p 76e3497:zarin/copilot.py` gives `_OUT_OF_SCOPE` and `_BUSINESS` byte-identical to `_LEGACY_OUT_OF_SCOPE`/`_LEGACY_BUSINESS` in retrieval.py, and the old `_plan` really did terminate in a generic summary. Counting that `fallback` as an answer rather than a refusal is the conservative choice and is the right call.
- The eval scores the deployed function, not a copy: `current_route = copilot.route_intent` (retrieval.py:83), and `route_detail` is the same function `_plan` calls. I confirmed my independent harness, the shipped eval and the live /api/copilot endpoint all agree on intent for the same question (e.g. «سود خالص...» → recovery in all three). Drift between eval and product is structurally impossible.
- Arabic ك/ي folding works exactly as documented. Substituting ک→ك and ی→ي across all 25 of my questions changed 0 of 25 routes and left exact accuracy at 18/25 — the normalisation claim in RETRIEVAL.md §3 holds for this half (it is only the ZWNJ half that fails).
- The similarity score carries real information on unseen data, which is what makes the three-way route/clarify/decline split defensible: on my 25 independent questions the nlu score median was 0.248 for correct routes and 0.155 for wrong ones (pipeline/_panel/checks.py). The five-outcome scorer that keeps `misrouted` separate from `missed` from `unsafe` is better methodology than the single blended accuracy most projects publish.

### Testing & quality assurance — 68/100

*Lens `testing-qa` · confidence high*

I ran the suite (180 pass, 8.3s), measured line coverage with a stdlib `trace` harness (82.7%, 2704/3270 lines under zarin/; 80.9% excluding 299 lines of pure eval-case data), and hand-mutated zarin/ 24 times, reverting each immediately. One mutation (Bearer prefix [7:]→[6:]) is an equivalent mutant because `.strip()` absorbs the space; of the 23 real ones, 10 were killed and 13 SURVIVED — a 43% mutation score. The kills cluster perfectly in money semantics and grounding (conv, recovered, the GMV cap, the peer quantile, evidence SQL, the retrieval ACCEPT threshold, two guard checks), which is where this suite is genuinely strong. The survivors cluster just as perfectly in security and restraint: `zarin.auth.issue`/`verify` are invoked exactly 0 times by the entire suite (measured by monkeypatched call counters), so deleting the HMAC signature check outright leaves 180/180 green; so does making `_ops_session()` return True, and flipping the public-deploy admin gate off. Every documented small-sample threshold (MIN_PEERS 5→2, MIN_SESSIONS_INSIGHT 100→1, MIN_SEGMENT_N 30→1) can be set to a fabricating value with a green suite. The frontend's 43 vitest tests pass locally but CI never runs them (`npm run build`, not `npm test`). And the routing safety property the suite asserts (`unsafe == 0.0`) is asserted only against the dev set the router was tuned on: on the holdout set the code itself calls "the honest number", unsafe = 0.05, and I reproduced both leaks live.

**Not higher because:** 13 of 23 non-equivalent mutations survived. The worst is that `zarin/auth.py` — the module written to close the previous panel's #1 CRITICAL (ZB-001, marked "✅ fixed in main") and the only thing gating the public Control Center — has literally zero test invocations: `if not hmac.compare_digest(sig, _sign(payload)): return None` can be replaced with `if False:` and all 180 tests pass. So can `def _ops_session(...): return True`. Every restraint threshold that backs the product's "never fabricate" honesty claim is unpinned. The frontend's 43 tests are dead weight in CI. There is still no coverage gate and no mutation gate (pyproject dev deps are pytest/httpx/ruff only — ZB-100, logged by the previous panel, verified still open), and provider.py's real HTTP transport is at 39.5%, worse than the 52% ZB-100 recorded. The suite also asserts a safety floor on the wrong dataset and the test is misnamed `test_held_out_...` while defaulting to the dev cases.

**Not lower because:** Where this suite aims, it hits hard. Ten mutations to the things that matter for money were killed in seconds: `session_status IN ('Verified','Paid')` for recovered, conv including paid_unverified, `_apply_gmv_cap` neutered, `_traces` always-true, the forbidden-payload check off, the evidence SQL blanked, quantile interpolation removed, ACCEPT 0.13→0.01, MIN_CUSTOMERS_RETENTION, and dropping the external_market safety family. The fixtures are deliberately engineered so assertions CAN fail (conftest S12 is a Paid-after-retry session that exists solely to make `test_recovered_is_verified_only` discriminating; the MHVF cohort is amount-tied so the ntile tiebreaker is actually exercised). `tests/test_query_determinism.py` binds a defect CLASS by static SQL analysis and carries its own `test_the_check_can_actually_fail` with 7 accept/reject shapes — that is better QA engineering than most production repos. `test_grounding_calibration.py` records known false positives with a deliberately inverted assertion rather than hiding them. All 13 test files pass standalone, so there is no order dependence. Ruff is clean. And `pipeline/validate.py`, which recomputes headline numbers from the raw 61 MB CSV with independently-written SQL, matched the live deployment 24/24 exactly when I ran it.

**Independently verified strengths:**

- Metric-correctness testing genuinely discriminates. Ten mutations to the money path were killed, most in under 4 seconds: changing recovered from `session_status = 'Verified'` to `IN ('Verified','Paid')` (pipeline.py:91) -> KILLED by test_recovered_is_verified_only; adding paid_unverified into conv (analytics.py:39) -> KILLED by test_paid_is_not_verified; neutering `_apply_gmv_cap` -> KILLED by test_opportunity_capped_at_realized_gmv; blanking the executed SQL in registry.evidence -> KILLED by test_opportunity_is_gap_based_with_honest_band; removing peers._quantile interpolation -> KILLED by test_quantile_interpolates_between_neighbors.
- The fixtures are engineered so the assertions CAN fail, which is rarer than it sounds. conftest.py session 12 is a Paid-after-retry session on M2 that exists for no reason other than to make test_recovered_is_verified_only discriminating (its comment says so), and mutating the rule to the Paid-inclusive one does flip that exact assertion. The MHVF cohort ties all 400+100 amounts specifically so ntile(5) needs its tiebreaker.
- tests/test_query_determinism.py binds a defect CLASS rather than the reported instance — a static regex over every zarin/*.py containing OVER — and it carries `test_the_check_can_actually_fail` with 7 accept/reject SQL shapes plus `test_there_is_something_to_check`, so the static check cannot pass vacuously. I ran the file standalone: 12 passed in 0.04s. This is better QA engineering than most production repos have.
- Test isolation is clean, so nothing here passes only by side effect of an earlier test. I ran all 13 files individually: 18+6+12+22+7+11+5+13+42+19+6+12+7 = 180, every file green on its own, despite shared DuckDB marts, a module-level response cache and a telemetry directory.
- The determinism and grain claims hold on the real 2.21M-row dataset, not just fixtures. I ran pipeline/validate.py's independently-written session-level SQL against the raw 61 MB CSV and compared to the LIVE deployment: 24/24 exact for M156/M43/M265, including retry collapse (55,940 sessions), paid_unverified kept separate (912 / 61,847,264,950 rial) and recovered = Verified-only (691).

## Round 1 — critical/high findings that survived adversarial re-check

Each was raised by one lens, then handed to a separate agent whose brief was to
**refute** it. Only findings that agent could reproduce appear here.

| # | Sev | Lens | Finding | Location |
|---|---|---|---|---|
| 1 | high | `architecture` | Response cache runs above the auth dependency: with REQUIRE_AUTH on, a warmed merchant URL is served to anyone, and marked publicly cacheable for a year | `zarin/cache.py:107-135 (middleware) vs zarin/api.py:120-135 (_merchant_scope Depends), api.py:36-37` |
| 2 | high | `code-quality` | ops_copilot.py never received the routing rewrite its own docstring claims it shares with the merchant copilot | `zarin/ops_copilot.py:32-118 (_plan), docstring at :1-7; live /api/admin/copilot` |
| 3 | high | `data-correctness` | Control Center merchant table silently ignores the selected window and shows 6-month lifetime figures under a windowed header | `zarin/api.py:352 (admin_merchants) → zarin/control.py:121 (merchants); frontend/src/ops/OpsOverview.tsx:27` |
| 4 | high | `data-correctness` | Changes page and the GMV-change insight card split the window differently: the same merchant's ΔGMV differs by 9.06 billion IRR on two screens | `frontend/src/pages/ChangesPage.tsx:28-30 vs zarin/insights.py:_card_change_alert (and zarin/copilot.py:_equal_halves)` |
| 5 | high | `statistics` | gmv_change alert asserts a causal driver at confidence "high" with no sample-size gate — live card built on 2 sessions | `zarin/insights.py:544-585 (_card_change_alert); live GET /api/insights?m=M336 and ?m=M186` |
| 6 | high | `statistics` | Amount quintiles are equal-count over tied prices: 84 published bands have lo==hi, and four "amount bands" containing the identical price report four different conversion rates | `zarin/analytics.py:120-131 (funnel bands_sql); zarin/insights.py:305-320 (_card_high_value_friction)` |
| 7 | critical | `reliability` | Response cache answers merchant routes before the auth dependency, leaking another tenant's money (defeats the recorded ZB-001/ZB-030 fix) | `zarin/cache.py:92-100 (middleware) vs zarin/api.py:36-37 (registration order) and zarin/api.py:121 (_merchant_scope)` |
| 8 | high | `reliability` | Anonymous callers can evict the request-telemetry ring and inflate the operator response 200x; "attention" is blind to 4xx | `zarin/obs.py:19 (record uses raw request.url.path), obs.py:22 (_trackable), obs.py:80-88 (attention rules)` |
| 9 | medium | `scalability` | Control Center performance telemetry is a per-instance in-memory ring and is blind to every CDN hit | `zarin/obs.py:15 (EventLog(..., maxlen=8000, durable=False)) surfaced at /api/admin/performance` |
| 10 | high | `product` | Ops copilot has no refusal path — every unmatched question returns the platform status line at confidence=high | `zarin/ops_copilot.py:117 (catch-all `return _system(f, t)`)` |
| 11 | high | `product` | Merchant copilot answers "how much money did I lose?" with total successful sales, at confidence=high | `zarin/nlu.py:375 (gmv intent anchor "چقدر پول", weighted x4)` |
| 12 | critical | `ux` | LLM polish rewrites the answer's TIME FRAME, so a 6-month total is shown to the merchant as "yesterday's sales" — with اطمینان بالا | `zarin/ai/gateway.py:206-240 (grounding_failure) → rendered by frontend/src/components/Copilot.tsx:88-96` |
| 13 | high | `ux` | «اطمینان بالا» growth verdicts and prescriptive advice generated from 2–15 transactions, for 59 of 343 merchants | `zarin/insights.py:543-590 (_card_change_alert), gate at :552, confidence at :586` |
| 14 | high | `ux` | Peer benchmarking applies a sample floor to the peers but none to the merchant being judged — 2 transactions earns four green "بهتر از ۱۰۰٪" badges with no caution | `zarin/peers.py:88-118 (mine has no floor; low_n at :117 counts peers only) → frontend/src/pages/PeersPage.tsx:55-75` |
| 15 | high | `design` | Every axis label on the flagship trend chart reads backwards in Persian | `frontend/src/components/charts.tsx:11 (dir="ltr" on the TrendChart wrapper) — rendered on /#/overview` |
| 16 | critical | `ai-grounding` | Grounding guard is permutation-blind: the LLM can rebind engine numbers to the wrong metric and pass as "grounded" — observed live as median→mean | `zarin/ai/gateway.py:196-220 (_traces / the _values loop in grounding_failure)` |
| 17 | high | `ai-grounding` | An empty LLM completion is "grounded" and the client swaps it in, blanking a correct answer | `zarin/ai/gateway.py:206-240 + frontend/src/components/Copilot.tsx:81` |

<details><summary>23 claims the verifier refuted</summary>

- **The two largest deploy artifacts are outside git and outside CI; the CDN-served SPA is an unverified manual copy of the tracked one** (`architecture`) — The mechanical facts reproduce exactly — public/ is gitignored, untracked, 1.1 MB, a hand-made copy of the tracked zarin/static, and CI touches neither it nor data/ — but both stated impacts, which are the load-bearing part of a "high", do not survive contact with the deployment. (a) The "second engineer" scenario is self-refuting: public/ is untracked, so a second engineer's clone has NO public/ at all, and every path public/ does not contain falls through to the function's catch-all over the git-tracked zarin/static (proved live: /nonexistent-route and /merchant/M156 both return Server-Timing and the current bundle). Their deploy is therefore correct, just CDN-cold for `/`; the only tree that can hold a stale public/ is the maintainer's own, whose recipe step 2 (`rm -rf public && cp -r`) is idempotent. (b) Even in the maintainer's stale case the divergence is loud, not silent: `/` would serve the old shell from the CDN while `/merchant/M156` serves the new one from the function — two app versions in one deployment, visible on the first deep link, not something "local testing passes on". (c) The marts half is a mischaracterisation: .vercelignore exists to stop the .gitignore fallback deleting marts that ARE in the working tree; a clone has no marts for any ignore file to save, because the anonymised payments dataset is deliberately uncommitted (committing it would be the worse finding, and CI runs on fixtures for exactly that reason). "Clone + vercel deploy --prod 500s" is skipping step 1 of a documented 4-step recipe whose §5 already warns that git-connected deploys ship no marts, and db.py fails with a named instruction rather than a bare 500. What remains is real but small: there is no guard making the copy unskippable. The lazy fix is one line in frontend/package.json — `"build": "tsc --noEmit && vite build && rm -rf ../public && cp -r ../zarin/static ../public"` — which also puts it under the existing CI frontend job. Nothing here is wrong money, a security hole, or a false claim; nothing is currently broken (verified by md5/etag against the live CDN, not by reading the recipe). Severity low, medium at the very outside — not high.
- **fa.py and fmt.ts round in opposite directions on ties; 2 of 343 live merchants render the same conversion rate two different ways** (`code-quality`) — The mechanism and the arithmetic are correct and I reproduced them byte-for-byte, including the exact "2 of 343" and the live M130 pair (overview conv=0.5625 → frontend ۵۶٫۳٪; copilot ۵۶٫۲٪) — and I found a case they missed (median_ticket ties at M121 812500 and M56 522500 → copilot ۸۱۲/۵۲۲ هزار vs Overview ۸۱۳/۵۲۳ هزار). But the finding's headline and its two load-bearing claims do not survive. (a) "One number, two renderings, side by side… the only place a user can see both formatters at once" is false: Copilot.tsx:93/96 gates the glance KPI row on `empty = turns.length === 0`, so the tile showing ۵۶٫۳٪ unmounts the moment the answer showing ۵۶٫۲٪ appears — the renderings are sequential across a tab switch, never co-visible. The one surface that genuinely renders both formatters together (Overview KPI tile + server-formatted InsightCard) is dead for these merchants: /api/insights?m=M130 returns zero cards. (b) "Exactly the ZB-043 defect the parity test was written to close, still live" is wrong: fmt.test.ts:1-13 enumerates separator, trailing ".0" and negative-sign, and I verified the first two are still fixed. Rounding mode was never in ZB-043's scope; this is a fourth divergence class, and the accurate criticism is only that the header's blanket "both sides now agree byte-for-byte" is overbroad. Also "43 tests" is 35. What remains after stripping the framing: a real display-consistency bug in which no money is wrong, no evidence/SQL/params diverge, the underlying deterministic value is identical, the delta is 0.1pp or 500 rial inside an already-compacted display, and it touches 4 of 343 merchants (the two conv ones having 16 total sessions across six months) — plus a genuinely confirmed process gap, that a written regression guard never runs in CI. That is a ticket, not a high. Fix is one rung: `roundingMode: "halfEven"` on fmt.ts's three Intl formatters plus a half-even replacement for Math.round, or make fa.py's `_max_frac`/`fa_num` half-up — and add one tie assertion (pct(0.5625,1)) so the guard actually guards, then wire `npm test` into ci.yml.
- **Peer percentile has no floor on the merchant's own sample size: n=1 yields a green "better than 100% of 13 peers" verdict on the live site** (`statistics`) — The mechanism reproduces exactly and I could not break it: zarin/peers.py floors the peer side twice (pool sessions>=500, per-peer HAVING sum(sessions)>=100) and defines low_n as len(vals)<8 — a PEER count — while nothing reads the merchant's own denominator, so live /api/peers?m=M164 (own sessions=1) returns four rows at percentile 100 with suppressed:false and low_n:false, and PeersPage.tsx:57 paints them chip-good with no caution. My independent sweep of all 343 merchants replicated the reviewer's counts to the digit (1687 published rows, 556 from <30-own-session merchants, 522 coloured, 362 at exactly 0/100, 187 merchants under 100 own sessions, median 69, min 1), and I found an extra propagation they missed: copilot.answer('M164','مقایسه با همتایان') returns confidence="high" alongside «صدک ۱۰۰ از ۱۳ همتا». What is overstated is the severity and the blast radius. No money is wrong: the money-facing path already has the own-side floor the peers path lacks — insights.py gates every card on ctx.me["sessions"] < MIN_SESSIONS_INSIGHT (100) at lines 246/256/267/308/415, and insights.generate('M164',...) returns zero cards, zero rial. The percentile is arithmetically correct and honestly described in the registry ("exact rank among peers ÷ n peers", not a distributional assumption); the defect is a missing uncertainty gate on a display surface, not a false number, which is the "high" band rather than "critical" under the stated rubric. The impact line "the majority of the merchant population" also does not survive: at the coin-flip threshold it is 116/343 merchants (34%) with <30 own sessions receiving a coloured chip, and 28 (8%) at n<=3; the 55% figure uses <100 own sessions, where a percentile is coarse but not a coin flip. Those 187 thin merchants carry 1,253 of 2,062,839 period sessions — 0.06% of the platform, i.e. the dormant tail. Genuine defect, correctly located, fixable with one own-n floor inside benchmarks() that covers PeersPage, copilot and the evidence drawer at once — but high, not critical.
- **The advertised peer-matching rule is silently discarded for 82% of merchants; the fallback pool's median peer is 45× larger and converts 30pp better** (`statistics`) — Every descriptive number in this finding reproduces exactly — the level split (281/43/19), the 81-merchant pool, the 45.2x/666.7x size ratios, the 0.5896-vs-0.2872 conversion split, pearson +0.3089, and M136's {cat, g:0, tk:0} params live. But each causal claim built on them is wrong, and the two that matter run backwards. (a) The bias mechanism does not exist: every peer at every level is drawn from the same sessions>=500 pool, and inside that pool pearson(log sessions, conv) is -0.0164 (spearman -0.028, conv flat across size quartiles at .575/.652/.586/.575). The +0.309 the finding cites is a pool-vs-outside artefact created by the >=500 floor, which applies identically at scale+ticket — the fallback does not introduce it. (b) The direct counterfactual refutes the baseline claim: across the 85 merchants where both peer definitions yield >=3 rated peers, the category fallback's peer p50 conversion is on average 0.6pp LOWER (median delta exactly 0.0000, higher in only 34/85). (c) The money claim inverts: recomputing the whole opportunity book under a forced tight band vs forced category gives category/tight = 0.9513 (33.9e9 vs 35.6e9 IRR), larger in 9 cards and smaller in 12 — the fallback deflates the book by ~5%. (d) The named "mechanism amplifier" contributes exactly zero rials: all 62 zero-gmv merchants have verified=0 (zero revenue in six months, so gmv_per_day=0 is the truth and a x1/4..x4 band around zero is undefined by construction), and `_apply_gmv_cap` demotes every rial estimate for a zero-GMV merchant to a zero-impact alert — I measured 15 such demotions. (e) The IRR is immaterial and misattributed: sub-500-session merchants account for 7.97e9 of a 5.06e11 book, 1.58%. (f) "Silently discarded" is false three times over — the cascade is documented in docs/ANALYTICS.md:47-48 directly beneath the very rule quoted as "the documented rule", in the peers.py docstring, returned as `group.level`/`group.rule_fa`, exposed with `where_sql`+`params` in the evidence drawer, and rendered verbatim by PeersPage.tsx:27; live M136 states "all active merchants in your category (at least 500 sessions)", not the tight band. The fallback's actual effect is coverage, not inflation: 144 cards exist only under it versus 3 only under the tight band, because `_gap_card` needs >=5 rated peers and the tight band rarely clears it — deleting the fallback would blank the peers page for four merchants in five without de-biasing anything, and the suppression floor (MIN_PEERS=5), the `low_n` flag below 8, and the Persian small-pool caution already fence the residual noise. What genuinely survives is one line of copy: the static PeersPage subtitle "مقایسه فقط با پذیرندگانی که واقعاً شبیه شما هستند — نه میانگین کل بازار" does not soften when `level === "category"`, so for a merchant whose peers are 45x larger it oversells what the category level delivers, sitting awkwardly beside the `rule_fa` two lines below that tells the truth. That is a one-line frontend copy fix, not a statistics defect — no wrong money, no false claim, no misleading number.
- **Operator/Control Center surface has no real authentication (self-mintable ops token behind a fake OTP login)** (`security`) — The MECHANISM reproduces exactly and I do not dispute it: anonymous POST /api/auth/session?scope=ops mints a valid ops token, GET /api/admin/platform with it returns 200 (343 merchants, gmv 5,165,464,690,799) while no token returns 403, auth_session performs no credential check, and Login.tsx's OTP form never validates the 5 digits. I mark this refuted because the IMPACT is materially overstated and the severity is inflated. The headline exposure the reviewer claims ("platform metrics, per-merchant rankings/GMV") is ALREADY 100% public with no token at all: /api/meta is unauthenticated and returns all 343 merchants with per-merchant gmv/sessions/verified, and summing it reproduces the "ops-gated" platform KPIs byte-for-byte (343 / 2,062,839 / 1,025,655 / 5,165,464,690,799), with the same GMV ranking /api/admin/merchants returns. The token's incremental disclosure is only obs/ai-ops telemetry (2 requests, $0 cost, no models) and data-source status (GA4 not_configured); a regex sweep for keys/secrets/paths/emails across all six ops payloads (18,083 bytes) found nothing. A working lockdown also already exists: with ZARIN_ADMIN_TOKEN set and REQUIRE_AUTH off, a self-minted ops token gets 401 — the deploy simply does not set the env var. The posture is deliberate and documented in-code (Login.tsx:12-13 "Demo gate: no auth backend in the challenge build, so any 5-digit code proceeds") and in memory.md:134 ("Single-tenant; no auth/RBAC enforced"), and the reviewer's own impact paragraph concedes this is an intentional demo rather than a breach — which is not a "high". Two things the reviewer MISSED that keep this at medium rather than low: (1) ZARIN_REQUIRE_AUTH=1, the documented hardening flag, actively REOPENS the bypass — with ZARIN_ADMIN_TOKEN set, flipping REQUIRE_AUTH on moves self-minted-ops from 401 to 200, because _admin_guard's "if REQUIRE_AUTH and _ops_session(...)" accepts a self-mintable token; turning the security knob on weakens the admin gate; (2) docs/EXPERT_REVIEW.md:353 records ZB-019 as "fixed in main" when the fix only moved the gate from 200-without-header to 403-then-mint-then-200, so the review record overstates the remediation. Those two deserve tickets. The UI copy ("log in with your ZarinPal account", no demo disclaimer on screen) is a fair but cosmetic complaint. Net: real defect, wrong severity, wrong impact — the operator surface guards data that is already anonymously public.
- **AI-Ops dashboard understates the LLM fallback rate ~10x and LLM p95 latency ~19x, because guard-rejected calls are recorded as source="deterministic"** (`reliability`) — The MECHANISM is real and I reproduced it exactly, twice — but the finding as written is overstated on three of its four load-bearing claims, and one half of its own title is not a defect at all.

CONFIRMED. `zarin/ai/gateway.py:286-297` sets `base.fallback=True` and attaches the real `comp.latency_ms/cost_usd/model/provider` while leaving `base.source=="deterministic"`. `zarin/ai/telemetry.py:53-54` then builds the latency population as `llm=[e for e in ev if e.get("source")=="llm"]`, so every guard-rejected call is dropped from p50/p95/p99. My synthetic repro emitted the claimed dashboard row byte-for-byte (llm_requests=2, deterministic=98, fallback=8, fallback_rate=0.08, p95=900). Better, I reproduced it LIVE on one warm instance: 8 real polish calls to nvidia/nemotron (6 guard-rejected, 2 accepted), after which /api/admin/ai-ops reported `llm_requests=2, fallback=6, p95=6638` — while the rejected calls I watched come back carried latencies of 6277/6675/7223/8294/10174/**18680** ms. `models` shows `count: 2` for a model that was actually invoked 8 times. The UI tooltip in `frontend/src/ops/OpsAI.tsx:42` is the sharpest proof: it promises «۹۵٪ درخواست‌های مدل سریع‌تر از این زمان پاسخ گرفته‌اند» ("95% of MODEL requests answered faster than this") next to p95=6638, when 6 of 8 model calls were slower. That claim is simply false, and it is the part of this finding that deserves a ticket. The reviewer also missed a gap that strengthens their own case: the provider_error branch (gateway.py:279-283) fires before any latency is captured, so an OPENROUTER_TIMEOUT (config.py:42, 20s) records `latency_ms=None` and contributes nothing to p95 either — the single worst case is invisible on any code path.

REFUTED / OVERSTATED. (1) "Understates the LLM fallback rate ~10x" is not a defect. `fallback_rate` is documented at `zarin/ops_copilot.py:39` as «fallback ÷ کل درخواست‌ها» (fallback ÷ ALL requests) and the UI bar is labelled «سهم پاسخ‌هایی که مدل زبانی کنار گذاشته شد» — share of all ANSWERS. It computes exactly what it says. "True rate = 0.80" is a different metric (per-LLM-attempt) the product never claims to show. A diluted denominator is weak metric design, not a miscomputation. (2) Both headline magnitudes are artifacts of the reviewer's own fixture — they chose 8 rejects at 17000ms, so "~19x" and "~10x" are properties of their test data, not measurements of the system. Live the real magnitudes are 2.8x (18680 vs 6638) and 3.25x. (3) Their live evidence does not reproduce: I got `fallback=0, llm_requests=0, p95=null` on a cold instance with no `/api/copilot/polish` row in /api/admin/performance at all — telemetry is per-serverless-instance and ephemeral, so `fallback=14/llm_requests=3` and `p95=4873 vs 15585.2` are unverifiable by anyone. (Their reasoning there was sounder than I expected — I measured provider lat=18680 against Server-Timing app;dur=18734.3, only 54ms of scope difference — but the numbers themselves are gone.) (4) "Hides the slowest calls entirely… an operator would never escalate" is contradicted by the same panel: `hallucination_risk=6` is computed over ALL events (telemetry.py:59), is correct, and renders directly beneath a stat reading «با مدل زبانی: ۲». Six risk events against two LLM requests is a glaring on-screen contradiction. The failure COUNT is honest and visible; it is the LATENCY and the unit cost that are wrong. (5) `cost_per_request` (5x overstated in my repro: 0.05 vs 0.01) errs toward alarm, not complacency, and is currently inert on a free model (cost_usd=0.0) — it mildly undercuts their own "would never escalate" thesis.

Severity: medium, not high. No money is wrong, no security boundary is touched, this is an internal operator panel, and its failure counters are accurate. What is genuinely broken is one percentile and one unit-cost divisor — worth a ticket (the honest fix is a `llm_attempted` flag, or selecting on `latency_ms is not None` instead of `source=="llm"`), not a ship blocker.
- **Response cache returns merchant data before the merchant auth dependency runs** (`scalability`) — The MECHANISM reproduces exactly — byte-for-byte, including the reviewer's own 22,378-byte figure — and I am not disputing it: `cache.middleware` (zarin/cache.py:94-107) keys on `path?query` only and returns from the `_get(key)` branch before `call_next`, so `Depends(_merchant_scope)` (zarin/api.py:121) never runs on a HIT; with ZARIN_REQUIRE_AUTH=1 a cold anonymous GET /api/overview?m=M156 is 401, and after one authenticated request the same anonymous GET is 200/HIT with an identical body, as is a request bearing an M18-scoped token, as is `Authorization: Bearer garbage`. The code should be fixed (two lines: skip the response cache and the `public, s-maxage` header whenever REQUIRE_AUTH is on, or fold the bearer claim into the key). But BOTH load-bearing halves of the write-up are wrong, and they are what carries it to "critical". (1) The premise "the product's own documented production posture (ZARIN_REQUIRE_AUTH=1, which is what the prod-shaped migration path calls for)" is false: the string `ZARIN_REQUIRE_AUTH` appears in zero files under docs/ and in no deploy config (vercel.json, Dockerfile, docker-compose.yml set only DATA_PATH/HOST). The documented production path is docs/DEPLOYMENT_SPEC.md:73-75 — "AuthN: none (single-tenant) → **OIDC**; sessions", "AuthZ → **RBAC** … tenant-scoped queries enforced at the boundary", "Multi-tenancy → tenant id on every row/query; isolation tests" — i.e. replace this layer, not flip this flag. Every mention of the flag (config.py:23-26, auth.py:11-15, api.py:124-128, api.py:228) explicitly calls it an opt-in demo seam. (2) The impact "the merchant isolation control is a no-op for every cached path" misattributes the cause. `POST /api/auth/session` (api.py:226-243) mints a signed 12-hour token for ANY merchant_key with zero credentials — no password, no OTP, no identity check anywhere in the file; `_check_merchant()` only asserts the merchant exists. I verified that with REQUIRE_AUTH=1 and a self-minted token an attacker gets 200 on a COLD cache (`cold overview, self-minted token: 200 MISS 22378`), on the route the cache deliberately excludes (`/api/copilot: 200`), and on the operator surface (`/api/admin/platform: 200` with a self-minted ops token). So under the flag the isolation control is a no-op on every path, cached or not, and the cache grants no access that is not already one unauthenticated POST away — it cannot be "what makes isolation a no-op" when the flag was never on. What survives is real but smaller than claimed: a cache whose key omits the identity dimension, plus `public, s-maxage=31536000` on a per-merchant body (RFC 9111 does permit a shared cache to store an Authorization-bearing request's response when `public`/`s-maxage` are present, so that sub-claim is mechanically right — the live edge does store these: `X-Vercel-Cache: HIT`, `Age: 200`, though it rewrites the downstream header to `public, max-age=60`). Zero exposure in the shipped configuration, zero marginal exposure in the flagged one, no deploy or doc that turns it on: that is a ticket, not a critical. I'd call it medium — high only under the counterfactual where someone gates token issuance properly and leaves cache.py untouched, which is exactly why it deserves the two-line fix now.
- **Throughput decreases under concurrency; p99 rises 18.7x from 4 to 32 in-flight requests** (`scalability`) — The measured *shape* half-reproduces, but every load-bearing claim built on top of it is wrong. (1) The named location is a red herring. I ran the identical app with the DuckDB budget set to 1, 4 (default) and 16 threads: conc=4/conc=32 throughput was 35/29, 33/25, 33/26 rps respectively — indistinguishable, and threads=1 was marginally the *best* with the lowest CPU per request (77 ms vs 107 ms at threads=16). `PRAGMA threads=4` at zarin/db.py:35 is not the ceiling. Corroborating this: the server process never exceeded ~45% of 8 logical cores at any concurrency, so it is not even CPU-saturating. (2) The real limiter is the single Python process, and it is a flag away. Same code, same pragma, `uvicorn --workers 4`: conc=8 gave 94 rps / p99 131 ms versus 28 rps / p99 598 ms on one worker; conc=32 gave 70 rps / p99 1542 ms versus 28 rps / p99 2920 ms — 3.3x throughput, 1.9x better tail. And the "peak then decline" curve persists at 3x the level with 4 workers (94 → 70 rps), so it is generic oversubscription, not a defect in db.py. (3) "Hard ceiling near 20 req/s" is a product of the driver, not the system. Every endpoint tested is in `cache.CACHEABLE` and ships `s-maxage=31536000`; the cache-busting parameter no real client sends defeats both layers. Without it, the same single-worker server did 68 → 370 → 530 → 625 → **636 rps** at conc 1/4/8/16/32 with p99 = 86 ms at conc=32, monotonically increasing, zero degradation — 23x the claimed ceiling, on a key space of 343 merchants against a 512-entry LRU. (4) The Vercel impact statement is contradicted by the live deployment: `/api/overview?m=M156` returns `X-Vercel-Cache: HIT, Age: 790` — the lambda is not invoked at all; and when I forced 32 concurrent cache-busted requests at the live origin, `Server-Timing: app;dur` stayed flat at 45 ms p50 / 155 ms p99, i.e. the origin never queued. That header does capture in-process queueing (locally at conc=32 srv_p50 1099 ms tracked client p50 1108 ms), so its flatness live is evidence, not an artifact. The 24.5 s-vs-maxDuration:60 worry does not describe the deployed system. (5) "p99 rises 18.7x from 4 to 32" is Little's Law, not a finding: 8x the in-flight requests at fixed throughput must produce ≥8x the wait. (6) "44 OS threads while only 4 DuckDB workers exist" reproduces (43 measured) but is Starlette's default 40-worker anyio pool for sync `def` routes — framework default, unrelated to DuckDB, and already recorded as ZB-072 (MEDIUM). (7) Novelty is thin: this is the residual of ZB-002 (docs/EXPERT_REVIEW_ISSUES.md:34, "Process-wide RLock serializes every DuckDB query — hard concurrency ceiling", measured 6.5 req/s, marked fixed). The fix took the uncached single-process ceiling from 6.5 to ~30 req/s; re-grading the leftover saturation curve as a new `high` is regression-by-relabelling. What genuinely survives is one sentence of the original: there is no admission control — `zarin/__main__.py:26` calls `uvicorn.run(...)` with no `--limit-concurrency` and no `--workers`. That is real, matters only on the self-hosted path, is fixed by two flags, and is `low`. Also worth noting the claimed absolute numbers are ~2x pessimistic against my hardware (their conc=1 7.5 rps vs my 12.6; their peak 19.6 vs my 29–43 across six runs, with 25.7–43.4 run-to-run spread at conc=4 alone — enough noise that "every step beyond 4 is slower" is not a supportable statement, and their own 10x dataset peaks at conc=8, contradicting it).
- **No DuckDB memory limit and a spill directory on a read-only filesystem; peak RSS is 11x at 10x data** (`scalability`) — Partly reproduces, but the two things that make it "high" are both wrong. CONFIRMED: `zarin/db.py:35` sets only `PRAGMA threads=4`; a fresh connection reports `memory_limit`=25.0 GiB and `temp_directory`=`.tmp`, and grep over the repo finds no other pragma. Memory does grow with data under in-process concurrency — my 32-concurrent mixed-endpoint load peaked at 240 MB on the 1x marts and 2,271 MB on a 10x mart set I built, worse than the reviewer's 154/1,720 MB. Setting `memory_limit='512MB'` cuts that 10x peak to 739 MB (fits a 1 GB function) for ~1.9x wall time, so the one-line fix works and deserves a ticket. REFUTED, in four places. (1) "11x at 10x data" is presented as amplification; it is linear — 10x rows, 9.5x memory in my run, 11.2x in theirs. (2) The spill half of the finding is inert and contradicts the memory half: at the 25 GiB default DuckDB never reaches its budget, so it never spills — after a full `/api/customers` at 1x and 10x plus three concurrent load runs, `.tmp` was never created in cwd. The "one escape valve on a read-only path" describes a branch that the finding's own configuration makes unreachable; the same one-line fix that sets the limit would set the temp dir. (3) "Hard OOM kill with no diagnostic" is wrong in both branches: with a limit low enough to force spilling and an uncreatable temp dir, DuckDB raises an explicit `IOException: IO Error: Failed to create directory "<path>"`, and a real container OOM on Vercel/Lambda is logged as `Runtime exited with error: signal: killed` with a Max Memory Used line. Forced spilling to a writable dir also just *worked* — the whole-platform 20.6M-row customers query completed inside a 143 MiB budget. (4) The failure is not reachable in the shipped configuration. Concurrency inside the process is bounded by Starlette's 40-worker anyio threadpool, so 1x peaked at 308 MB under 96 concurrent — only 1.28x the 32-concurrent figure; it cannot climb to 1 GB at the shipped dataset no matter the arrival rate. And the 10x scenario cannot deploy on the target the finding cites: my 10x marts are 275 MB of ZSTD-15 parquet (artificially compressible — duplicated rows dictionary-encode far better than real data, so a realistic 10x is 400-630 MB), `vercel.json` ships `data/marts` inside the function, and Vercel's unzipped serverless limit is 250 MB. At 10x this fails at build time, not at OOM time, and the answer is not a pragma. One unresolved point the finding also did not check: the 25 GiB it quotes came from the reviewer's 31 GiB dev host. Vercel Python functions run on Firecracker microVMs whose guest kernel reports MemTotal ≈ the configured function memory, so DuckDB's 80%-of-physical default in production is plausibly ~80% of the function allowance, i.e. roughly the back-pressure the finding says is absent. No endpoint exposes `current_setting`, so I could not settle it live — but the finding asserts the opposite without evidence. Minor: the location is db.py:35; 33-34 are a comment. Medium, not high: a real "SET memory_limit from the container allowance" ticket, not a production-blocking OOM.
- **Two of the five /api/customers queries ignore the selected date window, so narrowing the period cannot reduce the cost** (`scalability`) — The mechanical observation is true and I reproduced its cost curve — interval and cohorts genuinely have no date predicate, they are ~40% of the endpoint at 1x, and they scale near-linearly with a merchant's history (12→62→289 ms from 1x to 10x to 30x). Everything after that is wrong. This is not an oversight the team missed: analytics.py:215 documents it in a comment, and the product surfaces it to the user twice — the cohort section sub-line says "computed over the whole data window, not just the selected period" and the median-interval tile carries a «کل بازه» chip. More importantly, the implied fix is a correctness regression, not an optimisation: I applied PERIOD_SQL to both queries on a 1-week window and the cohort grid collapsed from a 6-month retention matrix to one k=0 cell, and the median inter-purchase gap went from 27.7 days (n=6707) to 1.0 day (n=31) — a merchant-facing number wrong by 27x. A lifetime-retention metric that changed when you narrowed the viewport would be the defect. The impact framing is also overstated in two ways: "no user action can reduce" is the intended semantics (these two answers must not move with the picker, and the other three queries do scale — 30.3→18.6 and 22.0→10.3 ms), and "per uncached call" quietly hides that /api/customers is in CACHEABLE with s-maxage=31536000 plus a 512-entry process LRU over immutable marts, so a cold compute happens once per (merchant, window) per deployment, not per user. At real current scale the cold live cost is 171-196 ms server-side, not a "600 ms floor". What survives is a genuine but modest note: cohorts and interval are the only per-merchant aggregates not precomputed into a mart the way merchant_daily is, so the window-independent portion of this one endpoint grows with per-merchant history and would want a cohort mart before ~10x. That is a medium-at-most ticket dressed up as a high-severity scaling defect, and the specific remedy the finding implies would break two numbers.
- **Largest merchant's headline opportunity is 100.0% of its entire GMV, rendered as a point estimate with no interval** (`product`) — The mechanic reproduces exactly — I confirm the numbers, including the pre-clamp distribution — but the finding is overstated on three counts and is a re-report of an already-logged open issue. (a) The headline is factually wrong: M31 is not the largest merchant. It is #2 by sessions and outside the top six by money; the actual largest, M156 (1,951B IRR, 17x M31, and demo entry #1 — the real first-click path), does NOT exhibit this, publishing a clean 98.2B–196.4B band with capped=false. (b) "No interval" is true, but "no caveat / the most confident presentation" is not: InsightCard.tsx:92 fires a chip-warn «سقف واقع‌بینانه» on precisely these cards, and insights.py:143-145 replaces the impact label with "this stage loses more than half of payments — fix the infrastructure first", so the card never tells the merchant it can recover that money; the reviewer's framed impact ("a merchant is told it can recover an amount equal to every rial it earned") does not survive reading the rendered label. (c) It is not new: docs/EXPERT_REVIEW_ISSUES.md:811 is ZB-060 with the same title, location, M265 evidence and recommended fix, graded MEDIUM, plus ZB-058 for the confidence-forcing half; commit 1eb964e closed only ZB-001..044+120, so these were never claimed fixed and re-reporting them at HIGH inflates a known MEDIUM. Scope also argues down: 9 of 343 merchants (2.6%) have a flat capped rial card at rank 1, and no money is wrong anywhere — GMV, sessions and rates are all correct; what is defective is the collapse of the [0.5, 0.75, 1.0] scenario band into one clamped figure and the broken-branch override that forces confidence="high" (ZB-058), i.e. confidence-in-the-problem being fed into a field that means confidence-in-the-estimate. Claim 5 is not undermined: "capped at realized GMV" is documented behaviour and the API exposes capped=true; the cap firing is the guard rail working. One point where the reviewer was too soft rather than too hard: the copilot path (`format_impact`) prints «۱۱۴٫۵ میلیارد ریال» with no cap caveat at all, so the chip does not protect the chat surface — that is the strongest version of this finding and it is still a medium.
- **Every merchant's financials — including row-level sessions — are readable by anonymous callers on the live deployment** (`business`) — The observations reproduce, but the finding's mechanism, its location claim and its impact framing are all wrong, and it silently re-reports two issues the panel record already closes. (1) LOCATION IS FALSE. "zarin/api.py, no auth dependency on merchant routes" does not survive reading the file: every merchant route takes `m: str = Depends(_merchant_scope)` (definition zarin/api.py:121-135; applied at :247 overview, :257 insights, :264 funnel, :271 customers, :278 peers, :285 changes, :295 copilot, :313 polish, :329 feedback, :410 evidence/sessions), backed by a signed-session issuer at :226-243. I flipped `ZARIN_REQUIRE_AUTH=1` and the gate works completely: all seven merchant endpoints 401 unauthenticated, a token bound to M18 gets 200 on M18 and 403 on M156 and on /api/evidence/sessions?m=M156, and an unbound merchant token is refused at issue (400). So "the capability to gate exists and was simply not applied to the merchant surface" is the opposite of what the code does — it is applied and enforced, and the live deploy runs the documented demo default (zarin/config.py:26). The gap between the live URL and a tenanted deploy is one Vercel env var, not the "single hard blocker to a pilot". (2) IT IS ZB-001 + ZB-030, both marked "fixed in main" in docs/EXPERT_REVIEW_ISSUES.md:20 and :424 — the brief required disclosing that and showing the fix is still absent; the reviewer did neither, and the fix is not absent. (3) DATA SENSITIVITY IS INFLATED. The source is already pseudonymous at source: `data/other_challenge_data.csv.gz` header is session_key,try_seq,terminal_key,merchant_key(M145),category_id,...,payer_card_key — no merchant name, no card, no customer identifier anywhere in the API. The quoted "per-transaction row" is {session_key, date, amount, outcome, PSP}; /api/customers returns only counts and cohort aggregates. This is a public demo over an anonymised challenge dataset, not "a PSP publishing its merchants' revenue". WHAT DOES SURVIVE, and it is sharper than what was reported: the tenancy fix is inert on the nine cacheable routes, because `zarin/cache.py:94-107` middleware runs BEFORE the route dependency and keys only on `path?query` — no identity in the key. With ZARIN_REQUIRE_AUTH=1 I warmed /api/overview?m=M18 with a valid M18 token, then repeated it with no Authorization header at all: HTTP 200, X-Zarbin-Cache: HIT, full GMV payload. It emits `public, s-maxage=31536000` unconditionally, so Vercel's shared edge would do the same for a year. Secondly, /api/meta is not gated even with REQUIRE_AUTH on — it still returns all 343 merchant_keys with sessions/verified/gmv/category, which is exactly the competitor-book enumeration ZB-001 named. Thirdly, no test touches the boundary: `rg -l REQUIRE_AUTH tests/` matches nothing across 180 tests, so this is unguarded. Severity as written is medium at most (documented, pseudonymous, no PII, one flag away); it earns high only through the defect the reviewer missed, which makes the ZB-001/ZB-030 "fixed" status untrue in the enforced configuration. Not critical: no real merchant data and no real tenant exists today, the ops surface is genuinely gated (live /api/admin/platform → 403), and nothing here produces wrong money.
- **At the weekly cadence the product claims for itself, 79% of merchants get an empty feed** (`business`) — The arithmetic reproduces exactly — every figure cited is correct — but the finding is refuted on its premise, its population, its characterisation of the evidence, and its novelty. (a) The 79% headline measures a 7-day window the product does not offer: ctx.tsx exposes only all/90d/30d with `all` as default, api.py defaults to the full dataset range, and `rg` finds no 7-day window anywhere in frontend, backend or docs. Worse, `_card_change_alert` returns None below 28 days by construction, so the single most common card is structurally excluded from the reviewer's window — a large part of the 538→98 card collapse is the engine correctly refusing to halve a 7-day window, not the engine going silent. (b) 160 of the 270 "empty feed" merchants at 7d had ZERO payment sessions in the window and 92 more had 1-29; only 5 had ≥100. At 30d, 92 of 172 had zero sessions and the whole zero-card set holds 1.35% of six-month GMV. Returning nothing to a merchant who transacted nothing is `insights.py:663` working, and it is the product's own stated principle («عدد مشکوک/کم‌نمونه حذف می‌شود»). (c) On the default window the six ≥100-session zero-card merchants are the platform's best converters — M97 at 77.6% conversion on 12,753 sessions, M100 at 82.3% — so "the engine can't produce insight for them" inverts what is actually happening. (d) gmv_change is not "an alert with impact 0": it carries risk_gmv=479.7B rial, an impact_label_fa naming that rial change, and a decomposed driver attribution; impact_low/high=0 is the deliberate encoding for "not a recoverable-money estimate", which is precisely the discipline product claim 5 demands, and calling the answer to the product's own headline question («چه تغییر کرد؟ چرا؟») filler misreads the field. (e) The IMPACT paragraph invents its premise — there is no subscription, pricing, retention or engagement claim anywhere in PRODUCT.md, README.md or PLATFORM_BOOK.md to underwrite. (f) Decisively, this is ZB-003 ("37% of merchants see an empty dashboard", business lens) re-reported without acknowledgement, which the record marks ✅ fixed — and the fix worked: 126/343 (37%) then, 75/343 (22%) now on the same window, a 40% reduction. The prior verifier had already made the same "'empty dashboard' is inaccurate" correction I found independently and had already downgraded it critical→high. The brief required verifying and saying so before re-reporting a fixed finding; instead the direction of travel was reported backwards. One genuinely new, small thing survives: Overview.tsx:94 has no zero-sessions guard, so a merchant with 0 transactions in the window is told «این خودش نشانه خوبی است» — tone-deaf, though the KPI strip directly above already shows 0/0. That is polish, not the claimed defect.
- **18.5% of the headline unverified money carries a confidently-wrong diagnosis: merchants whose unverified total exceeds their entire realized revenue** (`business`) — Every mechanical fact reproduces bit-for-bit — 23 merchants, 21,505,478,359 IRR, 18.45%, M48/M30/M136 figures, rows #2 and #5, and the exact card payload. The reviewer did real work. But the three load-bearing characterizations are wrong. (a) "Confidently-wrong diagnosis" is false: the emitted text says the verify/callback from the merchant's store did not complete, which for M30 is simply TRUE. The reviewer's stated "real situation" — "your verify integration has never worked once" — is a strict specialization of the emitted diagnosis, not a contradiction, and the emitted action (check the verify error log for timeout/network/invalid-response; manually settle from the dashboard meanwhile) is the correct first step for a never-worked integration too. The defect is missing escalation, not wrong content. `confidence:"high"` is likewise not a defect — it qualifies the AMOUNT, which is a measured settled sum (all 69 M30 sessions carry settled_at), and the reviewer conflates confidence-in-the-number with confidence-in-the-diagnosis. They also cite `_apply_gmv_cap` as a location while conceding the exemption is right, so half the cited location is a non-defect. (b) The 18.5% headline uses the wrong proxy and overstates the described failure mode by 2.1x: pua>gmv is not "verify never worked". Only 14 merchants / 10,215,833,359 IRR / 8.77% have zero verified sessions ever; the other 9 merchants / 11,289,645,000 / 9.69% have demonstrably working verify — M319 has 621 verified sessions and 769M GMV and lands in the bucket only because 55 unverified sessions were high-ticket, so its diagnosis is plainly correct. Worse, the reviewer's own flagship example M48 sits in that working bucket (n_ver=4), which makes "your verify integration has never worked once" literally false for their row #2. (c) The impact narrative requires an operator who is blind to the adjacent column. `/api/admin/merchants?sort=unverified` is only the API-side fallback; the actual operator surface defaults to `sort="gmv"` (OpsOverview.tsx:26), and its table renders gmv immediately left of the unverified column (lines 119-120), so the row claiming 8.1bn unverified states 1.4M in sales beside it. The merchant workspace does the same — gmv=0, verified=0, conv=0.0 in the KPI tiles above the single card. "Lifetime revenue" is also loose: 1.4M is the 6-month dataset window. What survives is a genuine but smaller gap: `_card_paid_unverified` has no escalation branch even though the codebase already implements exactly that pattern one function over (`broken = mine > 0.5` at insights.py:140-146, which M30's 82% unverified rate would trip), and because M30 is under MIN_SESSIONS_INSIGHT=100 that single card is the only card it gets. One condition — zero verified in the window → escalate the wording and name the owner — closes it, copying a pattern already in the file. That is a ticket, not a ship-blocker: the money is real and correctly exempted from the cap, the diagnosis is true, and the contradicting context is on screen in the neighbouring column. Medium, not high.
- **The flagship 147bn IRR opportunity is inflated ~44% by a peer baseline containing a merchant the project itself documents as broken** (`business`) — The arithmetic reproduces to the last digit, but the finding's causal claim — the load-bearing part — is wrong, and I can show it. Recompute in-bank abandonment on the denominator that actually isolates the stage (`abandoned_inbank / sessions-that-reached-the-bank`) and M265 goes from the *best* peer (0.0365) to the *worst* (0.6535), and the peer median is **0.3388 with M265 and 0.3388 without it** — removing the "broken" merchant moves the correct baseline by exactly zero. So the 147bn is not inflated "by including a structurally broken merchant"; it is inflated by a per-session denominator that every merchant shares, M156 included, which conflates upstream drop-off with in-bank behaviour. A peer health filter — ZB-059's recommended fix and this finding's implied one — does not touch the defect. Three further overstatements. (a) The remedy number is impossible: `MIN_PEERS=5`, so dropping M265 leaves 4 peers and I verified by patching `peer_group` that the card *disappears* rather than re-pricing to 102bn; the reviewer's "clean baseline" is a figure the engine can never emit. (b) "Biases in precisely the direction that enlarges the claim" implies systematic pro-vendor bias — on `no_attempt_rate` in the *same* group M265 raises the peer median from 0.0760 to 0.0935 and makes M156 look better; ZB-059 already frames this correctly as a rank-shift that "can flip the 2pp card trigger either way". (c) "The largest rial number the merchant workspace shows" is false — GMV 1,951,081,768,030 is a top KPI on Overview.tsx:52, 13x larger. "Nothing discloses" is also loose: `rule_fa` states the category fallback verbatim on the Peers page, the evidence drawer emits the literal SQL with no scale predicate, and both the low_n chip and confidence=low render on the card. Finally, this is a re-raise: docs/EXPERT_REVIEW_ISSUES.md ZB-059 logs the identical mechanism on the identical peer group at MEDIUM and is not marked fixed. What survives, and should be re-filed under the right cause: the 147bn genuinely is overstated — on the stage-conditional gap (0.3756 vs 0.3388 = 3.7pp, not 8.76pp) the mid estimate lands near 58bn, so the real inflation is ~2.5x, larger than the claimed 1.44x but for a reason the finding never identifies. Given it lands on an explicitly-labelled scenario band («نه بازه اطمینان آماری») at confidence=low with two uncertainty chips rendered, and duplicates an open MEDIUM, HIGH is inflated.
- **ZB-032 is only half-fixed: `clarify` refusals still render as confident answers and their suggested questions are silently dropped** (`ux`) — The mechanism reproduces; the characterisation does not, and the severity is two bands too high. Confirmed: `outOfScope` at Copilot.tsx:135 is `intent==="fallback"||intent==="out_of_scope"` with no "clarify", and the live bundle carries that byte-for-byte, gating both the `.suggest` buttons and (inverted) the ConfChip on it. Everything the reviewer built on top of that is wrong. (1) "Render as confident answers" is false: the clarify `answer_fa` LEADS with «پرسش شما را با اطمینان کافی تشخیص ندادم.» — "I did not recognise your question with sufficient confidence" — as its own first sentence. There is no number, no business summary, no evidence button (evidence=[]). The missing bold `<p>` would be a redundant restatement of a sentence already on screen, not a missing disclosure. The ZB-032 harm (a plausible-looking answer to a question you did not ask) is structurally absent here — the fallback branch it was raised against returned real KPIs; clarify returns nothing and says so. (2) "Their suggested questions are silently dropped / the UI throws them away" is false: all three retrieved questions are rendered verbatim in guillemets inside the bubble text. The reviewer's DOM probe counted `button` elements and generalised that to visibility. The accurate defect is "not tappable", which is a smaller claim. (3) The ConfChip reads «اطمینان پایین» = LOW confidence, sitting on text that says "I am not confident which question this is". That is redundant, not misleading — the inverse of ZB-032, where a *medium* chip vouched for a wrong answer. (4) Frequency: clarify fires on 3 of the 249 curated dev+holdout questions (1.2%), and those sets are deliberately loaded with adversarial/colloquial/boundary families. The reviewer's own two examples do route to clarify, so the branch is reachable with plausible merchant phrasing — but 2/20 is not the rate. Also: the cited bundle `index-B_J9b7P6.js` is not what is served; the live asset is `index-LuObPBrS.js` (629,243 B, which does match their "628KB" and their exact quoted minified string, so this is a transcription error rather than fabrication). Residual real defect, worth a ticket: three tap targets are missing on a branch whose sibling has them, plus one redundant chip — a ~10-character diff (add `|| intent === "clarify"` at :135, which line 76 already does). Framing it as "ZB-032 is only half-fixed" misdescribes it: ZB-032's fix did not regress; `clarify` is a new intent introduced alongside it whose UI condition was not extended. The prior panel's own verifier downgraded ZB-032 itself from high to medium on the reasoning "no wrong data is shown"; the clarify path is strictly weaker than the one they downgraded, so high is indefensible. Low.
- **The most prominent money figures in the product carry no currency unit at all** (`ux`) — The mechanism reproduces exactly — `rial(v, false)` is shipped bare on the copilot landing glance, on every insight-card money headline and range, and (a surface the reviewer missed) in the evidence drawer's «مبلغ» column — but the finding as written is overstated in its headline, wrong in its central impact example, and inflated in severity. The title says "the most prominent money figures in the product carry no currency unit at all"; that is false. The most prominent money figures are the Overview KPI tiles, and `Overview.tsx:52,67` render `{rial(k.gmv,false)}<span className="u">ریال</span>` — verified in the deployed bundle as `ce(a.gmv,!1)` followed by `{className:"u",children:"ریال"}`. The Ops platform KPIs do the same, the funnel table puts the unit in the column header, and the chart tooltip uses `rial(v)` with the unit. The real pattern is a convention — state the unit once per container — applied in five containers and forgotten in three, which is a consistency bug, not an absent unit. The impact paragraph then picks the one example that refutes itself: "the number they are most likely to act on (paid-unverified) is the one shown bare on both the landing strip and the card." On `/#/overview` that figure is rendered *above* the cards, in a callout, as «۹۱۲ پرداخت به مبلغ ۶۱٫۸ میلیارد ریال» (`Overview.tsx:77`, `rial()` with unit), and the copilot's own answer for that intent returns «مجموع ۶۱٫۸ میلیارد ریال». So the merchant acting on paid-unverified sees the unit in both places they would actually act from; only a user who never scrolls past the chat landing strip sees it unlabelled. "The product contradicts itself" is likewise inflated — it omits a unit in some places and states it in others; it never states a *different* unit, and `تومان` has zero occurrences in `frontend/src`, so the product never invites the toman read by name. Finally this is neither new nor a regression: both call sites predate the reviewed commit 75de6bb, and the previous panel already logged the same underlying risk as ZB-085 (rial-only, no on-screen currency note, no toman toggle), still unfixed. The residue is genuine and worth a ticket — a one-token fix (drop `, false`, or add the `<span className="u">ریال</span>` the sibling components already use) on three surfaces, one of which is the default landing screen — which is medium, not high: it never produces a wrong number, never asserts a wrong unit, and every neighbouring rendering of the same figure names ریال.
- **Tooltips never open on tap or click — on a phone the entire plain-language glossary is unreachable** (`ux`) — The observable reproduces exactly — mobile tap 0/0/0/0, desktop click 0/0/0/0, hover 1 — so the tooltip genuinely does not stay open on a pointer press, and the code comment at Tooltip.tsx:45 ("Opens on hover, keyboard focus, and tap") is false. But the finding is wrong in its cause and overstated in its impact and severity, on three independently checked points. (a) The stated mechanism — "onFocus fires first and calls open(), then onClick reads the fresh box and closes" — is not the cause. My event log shows `mouseenter` is what opens it (Chrome fires the compat mouse sequence on a tap), and tap #2 fires NO focus event at all yet still ends at 0 pops. So the culprit is `onMouseEnter={open}` colliding with the `onClick` toggle; deleting or reordering `onFocus` fixes nothing, and neither does deleting `onMouseEnter` alone (focus would then open and click would still close). The real fix is the toggle itself — gate hover-open behind `matchMedia('(hover: hover)')`, or ignore a click that lands in the same gesture that just opened the box. (b) "the entire plain-language glossary is unreachable / the product's answer to 'what does نرخ تبدیل / کوهورت / پنجک mean?' exists and cannot be opened" is false for two of its own three examples and mitigated for the third: «کوهورت» is explained in always-visible Section `sub` text directly under the heading (CustomersPage.tsx:62); «پنجک» has no `<Term>` anywhere in the codebase — there is nothing to open, so the tooltip bug is not why it is unglossed; and «نرخ تبدیل» has an adjacent `.ev-btn` measuring 24x44 that I tapped successfully on the same mobile context, yielding a Persian definition, formula, grain, sample size and SQL. The same holds on the funnel, where the evidence definition spells out NoAttempt and its distinction from a bank error — i.e. the exact substance of TIPS.noattempt. Every page carrying `<Term>` also carries an `EvBtn`, so the register that is lost on touch is the friendly three-part gloss, not all explanation. (c) The tap-target line cites 44px as "the touch minimum"; 44px is WCAG 2.5.5 Target Size (Enhanced), AAA. The AA bar is 2.5.8 at 24x24, which carries an explicit exception for targets inline in a block of text — which is precisely what a `.term > .lbl` is. At 19-21px they sit under 24 but land in the exemption; and the actual controls next to them are already a deliberate 24x44 (see the ui.tsx EvBtn comment about a "real ≥24×24 icon hit area"). Also note the desktop half of the evidence is a non-impact: hover returns 1, so a mouse user reads the tip and the click merely toggles it shut, which is defensible. So: real defect, worth a ticket, wrong diagnosis, and the touch-user harm is a degraded explanation register rather than an unreachable one. Medium, not high — no wrong money, no security, nothing misleading; and it is adjacent to but genuinely not recorded by ZB-091, which the reviewer got right.
- **Signed deltas paint the +/− on the wrong side of the digits — and the test file claims otherwise** (`design`) — The core mechanism reproduces — I could not refute it — but two load-bearing supporting claims are wrong, and the severity is inflated. CONFIRMED: `pp()` (frontend/src/fmt.ts:32) and the Waterfall label (frontend/src/components/charts.tsx:122) hand-concatenate a bare U+2212, and `zarin/fa.py` emits a bare ASCII U+002D — none of the three prefixes U+200E. Node's own ICU disagrees: `Intl.NumberFormat('fa-IR',{signDisplay:'always'}).format(-1.4)` = `200e 2212 6f1 66b 6f4`, i.e. CLDR deliberately inserts the LRM for fa. Working the UBA by hand on "−۱٫۴ واحد" under `<html dir="rtl">` (frontend/index.html:2, and `.num` in theme.css:62 sets only tabular-nums, no direction): U+2212 is bidi class ES, W4 does not apply (no preceding EN), W6 turns it to ON, N1 puts it between sos=R and an EN-treated-as-R so it resolves R at level 1 while the digits sit at level 2 — L2 reordering yields visual "دحاو ۱٫۴−", sign to the right. Same derivation with the LRM prepended: W7 flips both ENs to L, N1 makes the sign L, the whole numeric run lands at level 2 and renders "دحاو ‎−۱٫۴". So the reviewer's per-glyph measurement is right, and fmt.test.ts:12 "Identical on screen in RTL" is measurably false. REFUTED — claim 1: "renders inconsistently with the Intl-formatted numbers sitting in the same card" is false in the shipped product. I checked all ~60 call sites of `rial()`/`pct()`/`faNum()` in frontend/src: every one receives a count, a GMV, a rate or a share (structurally non-negative), or is explicitly wrapped in `Math.abs()` — ChangesPage.tsx:65 `rial(Math.abs(d.delta_gmv))`/`pct(Math.abs(rel))`, Overview.tsx:22 `pct(Math.abs(d))`, CopilotPage.tsx:20 `pct(Math.abs(dp))`, charts.tsx:122 `rial(Math.abs(it.value))`. The LRM-emitting negative branch of Intl is exercised only by fmt.test.ts:69/96/133. Nothing on screen anywhere in the product carries an LRM'd sign, so there is no mixed rendering in one card; every sign the product draws is hand-prefixed and every one lands on the same side. The real inconsistency is between the shipped UI and a unit test's expectation, which is a smaller thing than claimed. REFUTED — claim 2: "affects every signed value in the product" overstates the blast radius. There are exactly three sign-emitting sites: fmt.ts:32 (`pp()`, used in precisely one place, ChangesPage.tsx:111), charts.tsx:122, and fa.py-generated sentences. The period-over-period deltas a merchant actually reads fastest — the ones the impact statement invokes — are arrow-coded, not sign-coded (Overview.tsx:22 and CopilotPage.tsx:20 use ▲/▼ with `Math.abs()`), so they are untouched. Both affected UI sites also redundantly encode sign as colour (`v >= 0 ? "var(--good)" : "var(--bad)"` at ChangesPage.tsx:111 and charts.tsx:121), so no reader loses the sign, only its position. SEVERITY: high is wrong. No number is wrong, no sign is lost, no money is misstated; the glyph sits where an unmarked bidi run puts it by default, which is what a large share of Persian web content looks like. It is a genuine non-CLDR i18n defect with a one-character fix (prefix U+200E in `pp()`, charts.tsx:122 and fa.py, or wrap in `<bdi>`), plus a test comment that misleads the next engineer. That is a ticket, not a ship blocker — medium, consistent with this panel's own calibration where purely visual defects with no data/correctness impact were downgraded from high to medium (docs/EXPERT_REVIEW_ISSUES.md:477).
- **Copilot gives a screen-reader user no announcement for 9.3 seconds and then an unreliable one (4.1.3)** (`accessibility`) — The structural core reproduces exactly and I confirm it: before asking there are zero live regions (`[]`); the pending bubble sits in no live region (`{"pend":true,…,"lr":[]}`); and the `aria-live="polite"` node first appears in the DOM in the same React commit as its content, which is the classic non-announcing pattern. But the two claims that turned this into a *new*, *high* finding are both wrong. (a) The "measured 9,286 ms" of silence does not exist. In two real-Chrome runs the deterministic answer landed at 679 ms and 1,295 ms; `Server-Timing` for `/api/copilot` is 6–200 ms. The 9.2 s figure is `/api/copilot/polish` (`app;dur=8769.7`, wall 9.2326 s) — the LLM rephrasing pass that runs *after* the answer is already on screen and that the pending bubble never covers. The reviewer attributed polish latency to the answer path, so the IMPACT paragraph's "hear nothing for ~9 s" is inflated ~10x; the real gap is under a second. (b) The "perverse" mechanism is wrong. The «در حال روان‌تر کردن بیان…» chip is *not* a later mutation of an already-present region: Copilot.tsx:70 and :77 are two setStates in one synchronous continuation, so React 18 auto-batching co-inserts the chip with the answer — proven in both runs, where the chip is already present in the very first snapshot that contains the answer. And in both runs polish never replaced the text (source chip stayed «محاسبه قطعی از داده شما»), so there was no announceable text mutation at all; the only later change is the chip's *removal*, which default `aria-relevant="additions text"` does not announce. So "the rephrasing is spoken but the original answer is not" did not happen. (c) The finding is not new: ZB-093 is an open, unfixed MEDIUM with the identical file:line, the identical "created in the same render as its content" mechanism, the identical pending-bubble observation and the identical 4.1.3 citation. The only fresh material offered was the measurement, and the measurement is wrong. Note also that the reviewer's evidence is DOM queries, not a screen reader — "which NVDA/JAWS do not announce" is asserted, not tested, exactly as in ZB-093, so it adds no verification either. Verdict: the defect is real and worth fixing (a persistent `<div aria-live="polite" aria-busy={pending}>` wrapper around `.chat` is a ~3-line change), but it is a correctly-classified MEDIUM that is already on the books, dressed up with a 10x-overstated latency number and an incorrect mechanism.
- **Metric tooltips fail 1.4.13 on both Dismissible and Hoverable** (`accessibility`) — Half of this finding is disproven and its headline impact sentence is factually false. I drove the live deployment with the same tool the reviewer used (real CDP `Input.dispatchMouseEvent` against Chrome 151 headless on https://zarbin-nine.vercel.app, merchant session seeded via `sessionStorage.zb_ws`) and reproduced their hover numbers exactly — tooltip 197px tall, 300px wide, `pointerEvents:"none"`, 10px gap, `activeElement = BODY`, and Escape does nothing on the hover-only path. But the claim "cannot be dismissed without moving the pointer" is wrong: I dispatched a real `mousePressed`/`mouseReleased` pair at the identical coordinate (929.09, 283.99) — the pointer never moved a single pixel — and the tooltip closed (`open:false`). That is literally the mechanism SC 1.4.13 Dismissible asks for ("a mechanism … to dismiss the additional content without moving pointer hover"). The reviewer stopped at Escape and never tried the click, even though `onClick={() => (box ? close() : open())}` is on the same line range they cite. Both keyboard paths also pass Dismissible: after a click the span holds focus and Escape closes it, and after a pure `\.focus()` (Tab-equivalent) the tooltip opens and Escape closes it. So Dismissible is REFUTED and only Hoverable survives. Hoverable does genuinely fail — panning the pointer onto the tooltip centre closes it — but the reviewer's named root cause is also wrong. `.tip-pop { pointer-events: none }` (theme.css:379) is not what breaks it: I forced `pointerEvents='auto'` on the live popover and it still vanished on pan. The actual cause is `Tooltip.tsx:39` `Math.min(window.innerHeight - 230, r.bottom + 10)` — a 10px dead gap — combined with `onMouseLeave={close}` on the trigger; the pointer dies 2px below the trigger, inside the gap, before it can ever reach the tooltip. Their recommended fix (drop pointer-events:none) would therefore not fix it. Two secondary overstatements: "affects all 21 TIPS definitions" conflates the dictionary size with rendered instances — I counted per route: overview 4, funnel 8, peers 2, customers 3, changes 4, quality 0, copilot 4, and it is one shared component, one defect, not 21. And the escalation to HIGH is unearned: ZB-091 already records this at MEDIUM, the reviewer's justification for going higher was "both Dismissible and Hoverable", and one of those two is disproven. What remains is exactly the MEDIUM already on the books. One point does go the reviewer's way: I checked whether the 1.4.13 Dismissible carve-out for content that "does not obscure other content" applies, and it does not — `elementsFromPoint` under the tooltip centre returns `BUTTON.prompt-card` with real text, so the popover does cover an interactive element. That makes 1.4.13 applicable, it just does not make the finding correct as written.
- **No skip link: content starts at tab stop 13 on every page, and 5 of 7 merchant pages have no h1 to jump to instead (2.4.1)** (`accessibility`) — The factual core reproduces exactly — there is no skip link in the source or in the 629 KB live bundle, the desktop Tab walk really does put the first in-`<main>` element at stop 13, five of seven merchant pages really do start at H2, the copilot h1 really does vanish after the first turn (1 -> 0), and login really has no headings and no main landmark. But the finding is overstated in three ways that change its grade. (a) The WCAG verdict is unsupported by the evidence offered: `page-has-heading-one` and `landmark-one-main` are tagged `["cat.semantics","best-practice"]` in axe-core 4.13.0 — neither is a WCAG rule. axe's actual 2.4.1 rule is `bypass`, and it did not fire; I checked why, and it is `inapplicable` because its matcher is `!!node.querySelector('a[href]')` and this SPA navigates with `<button>` only. So axe neither passed nor failed 2.4.1 — the reviewer inferred a WCAG A failure from two best-practice rules. Independently, WCAG's own sufficient techniques for 2.4.1 accept ARIA11 (landmark grouping) or H69 (headings at the start of each section) in place of a skip link, and both are present and verified: ASIDE/NAV(labelled "بخش‌ها")/HEADER/MAIN on all seven merchant pages, with axe's `region` rule passing, meaning all content sits inside landmarks. (b) The IMPACT sentence "no heading-navigation fallback on the majority of pages" is simply false — funnel exposes six H2s, quality H2+3×H3, changes H2+2×H3, peers H2+H3. Screen-reader heading navigation works on every page; what is missing is only the *level-1* heading, which is a different and much smaller thing than the absence of a fallback. (c) "12 redundant tab stops on every entry" is desktop-only: at 390 px the sidebar is not rendered and the bottom nav sits after `<main>`, so I measured 5 stops before content, not 12. What survives is real and worth a ticket: sighted keyboard-only and switch users on desktop genuinely pay 12 stops per navigation, and that is precisely the gap landmarks do *not* close, plus five pages and the post-turn copilot lack an h1. Both are one-line fixes — App.tsx:131 already renders the page title as `<div className="t-title">{current.label}</div>`, and promoting that single div to `<h1>` clears `page-has-heading-one` on all seven merchant pages and the copilot at once. No wrong money, no security exposure, no false product claim; a serious team fixes this in one commit but does not block a release on it. Medium, not high.
- **AI-Ops publishes routing_unsafe_rate = 0.0 from the DEV set while the project's own holdout measures 0.05 — the number the code comments call "laundered"** (`ai-grounding`) — PARTIALLY REPRODUCES, MATERIALLY OVERSTATED — downgrade to medium, do not drop. The mechanism is real and I confirmed every number: `zarin/ai/eval/runner.py:70` calls `retrieval.compare()` bare, `def compare(cases=RETRIEVAL_CASES)` defaults to the dev set, and the live ops endpoint serves `routing_unsafe_rate: 0.0` with `routing.n: 120`, `out_of_scope.n: 25` — while `compare(HOLDOUT_CASES)` gives unsafe 0.05 over 40 out-of-scope rows, with exactly the two «دلار امروز چند شد؟…» / «طلا گرون شده…» → `priorities` rows the reviewer names. The "laundered" comment at retrieval.py:154-158 is verbatim. But three load-bearing parts of the write-up are wrong. (1) THE CAPTION CLAIM IS FALSE. The reviewer says the UI note «مستقل از بانک نمونه‌های موتور و بدون دیدن کد نوشته شده است» "describes the holdout, not the set actually being scored." It describes both. `retrieval_cases.py`'s own provenance docstring states the dev questions were written by four independent labelling agents given only English intent descriptions and "explicitly denied access to the repository — so none of them ever saw `nlu.BANK`, the regex rules, or any example phrasing"; I checked verbatim overlap against `nlu.BANK` (188 phrases): dev 1/120, holdout 0/129. Both caption assertions hold for the set being scored. The caption is UNDER-QUALIFIED (it omits "and this set's failures were read while tuning"), not a description of a different set — a much smaller sin than the one alleged. (2) "PUBLISHES THE FLATTERING NUMBER" IS 1 OF 3. The same payload's `routing_misroute_rate` is 0.0316 from dev vs 0.0225 on the holdout — the published figure is ~40% WORSE than the honest one, on the axis the code itself calls "the worst failure mode for a business tool" and the UI labels «خطای پرهزینه». `routing_accuracy` differs by 0.5pp (0.9583 vs 0.9535). The dev set is a smaller, differently-composed sample, softer on one indicator and harsher on another — not a systematically flattering one. (3) THE 0.000 IS AN UNDERPOWERED SAMPLE, NOT PROOF OF LAUNDERING. Dev has 25 out-of-scope rows, so the metric's resolution is 0.04 — it cannot express 0.05. P(0 unsafe in 25 | true rate 5%) = 0.277. And dev DOES contain the same attack shape («قیمت دلار امروز چنده؟ می‌خوام بر اساسش تصمیم بگیرم قیمت‌هام رو عوض کنم یا نه»), which the router routes correctly; it fails on two near-identical holdout phrasings. Also, the impact sentence's arithmetic is wrong: "2 of 40 currency-price questions" — the 40 is the holdout's out-of-scope total across six families (safety 24, boundary 7, plain 3, paraphrase 3, colloquial 2, adversarial 1), not 40 currency questions. (4) NOT CONCEALED. `docs/EVALUATION.md` headlines the holdout (line 22 "129 blind Persian questions", line 25 unsafe 0.050), tables both sets side by side (lines 65-71: dev 0.000 / holdout 0.050), carries the honesty note at line 84, and names this exact gap as open residual risk in §6 item 1: "`unsafe` is 0.050, not 0.000 on the holdout — two out-of-scope questions still receive a data answer." ROOT CAUSE is a naming collision, not deceit: both files title themselves held-out (`retrieval_cases.py` line 1: "Held-out intent-routing evaluation set — 120 Persian questions"), and both landed in the same commit 5acf9e1 as runner.py's routing block, whose comment "the 120-question held-out set" faithfully echoes the dev file's own self-title. NOT NOT-A-DEFECT: a live operator surface does show 0% out-of-scope answering under a caption that omits the one qualifier that matters, when the project's own honest measurement is 5%, and the fix is one argument (`retrieval.compare(retrieval.HOLDOUT_CASES)`) plus a set label in `note_fa` and `OpsAI.tsx:82`. NOT HIGH: no money is wrong, nothing merchant-facing is misstated, the honest number is published and listed as open risk, the caption is true-but-incomplete rather than false, and one of the three published indicators is pessimistic against the honest set.

</details>

---

## Method (hand-written)

Each round is one run of `pipeline/panel.js` through the Workflow orchestrator:

1. **Judge.** Sixteen expert lenses run *in parallel and in isolation* — no lens sees another
   lens's output, so their agreements are independent rather than anchored. Each is given the
   repository, a live deployment to probe, the commands to run it, and the product's own
   claims, and is told to assume the product is worse than it looks. Each returns a score in
   0–100 with an explicit *not higher because / not lower because*, and up to ten findings,
   each needing a location and reproducible evidence.
2. **Verify.** Every `critical` and `high` finding is handed to a **separate** agent whose
   brief is to **refute** it, defaulting to refuted when it cannot be reproduced. Only
   findings that survive are recorded. This is the step that keeps a panel of language models
   from grading on plausibility.

The two rounds are the same panel definition run against two commits, so the comparison is
like-for-like. The round-2 lenses were not shown round-1's scores.

## What this evaluation is not (hand-written)

- **The panel is language models, not people.** They read code, run commands and probe a live
  deployment, which makes their findings checkable — every one carries a location and a
  command. It does not make their *scores* calibrated against human experts. Treat the
  deltas as more meaningful than the absolute numbers.
- **No human usability testing.** UX, design and accessibility are assessed from source,
  computed contrast ratios and rendered output. No Persian-speaking merchant was observed
  using the product.
- **The rubric lens is inside the mean.** It scores the same product against a different
  instrument, so it is partly correlated with the other fifteen.
- **A round-2 lens judges a product that was changed in response to round 1.** That is the
  point, but it also means round 2 is not an independent second opinion of round 1 — it is a
  re-measurement after targeted repair, and some of the repair was aimed at exactly what
  round 1 measured.
- **Scores move with panel composition.** A different set of sixteen lenses would land a few
  points either way.

<sub>Generated 2026-08-22 by `pipeline/gen_evaluation.py`.</sub>
