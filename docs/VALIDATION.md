# اعتبارسنجی تحلیلی و فنی | Validation

«صفحه رندر می‌شود» اعتبارسنجی نیست. سه لایه اعتبارسنجی مستقل انجام شده است.

## 1. تست‌های صحت متریک (fixture قطعی)

`tests/` — دیتاست مصنوعی کوچکی که همه حالت‌های خطرناک را پوشش می‌دهد
(`tests/conftest.py`)، و ۱۶ تست که اشتباهات مرگبار متریک را قفل می‌کنند:

- تلاش‌ها هرگز به‌جای جلسه شمرده نمی‌شوند (۹ ردیف تلاش → ۸ جلسه)
- تلاش مجدد GMV و تعداد تراکنش را چندبرابر نمی‌کند
- NoAttempt خطای بانکی حساب نمی‌شود
- Paid موفقیت حساب نمی‌شود؛ مبلغ آن جدا گزارش می‌شود
- «موفقیت اولین تلاش» هرگز از تبدیل نهایی بیشتر نمی‌شود (تعریف Verified-پایه)
- محاسبه مشتری تکراری merchant-scoped است (کارت‌های دو پذیرنده هرگز ادغام نمی‌شوند)
- تجزیه LMDI دقیقاً جمع می‌شود؛ تجزیه Δتبدیل نیز هویت جبری دارد
- با استخر همتای کوچک، هیچ صدکی جعل نمی‌شود (سرکوب کامل)
- فرصت = فرمول شکاف، نه جمع مبالغ ناموفق (تست ساختاری فرمول)
- شکاف کمتر از 2pp کارت نمی‌سازد
- برچسب adjusted_fee همیشه «شاخص نسبی» است

آخرین اجرا: **16 passed** (`uv run pytest -q`).

## 2. QA تحلیلی مستقل روی داده واقعی

`pipeline/validate.py` — اعداد سرخط برای سه پذیرنده با پروفایل متفاوت، با SQL مستقل
**مستقیم از CSV خام** (بدون عبور از marts یا کد اپ) بازمحاسبه و با پاسخ زنده API مقایسه شد:

| پذیرنده | پروفایل | چک‌ها |
|---|---|---|
| M156 | بزرگ‌ترین GMV، تیکت سنگین، ۹۱۲ پرداخت تاییدنشده | sessions=55,940 · verified=30,508 · GMV=1,951,081,768,030 · recovered=702 · همه ✔ |
| M43 | اصطکاک/نجات بالا (۹,۷۴۵ جلسه نجات‌یافته) | ۸ چک ✔ |
| M265 | یکپارچه‌سازی خراب (NoAttempt=6,447 از 6,828) | ۸ چک ✔ |

نتیجه آخرین اجرا: **ALL CHECKS PASSED (24/24)** — خروجی کامل در همین سند بالا قابل بازتولید است.

## 3. صحت ساختاری در زمان ساخت marts

`zarin/pipeline.py` قبل و بعد از ساخت assert می‌کند:
- یکتایی `(session_key, try_seq)`؛ سازگاری درون-جلسه‌ای merchant/amount/status/terminal؛
  عدم اختلاط NoAttempt با تلاش واقعی؛
- تعداد جلسه‌های mart = تعداد جلسه‌های خام؛ هیچ ردیف تلاشی گم نمی‌شود.

## 4. گیت‌های فنی

| گیت | فرمان | آخرین نتیجه |
|---|---|---|
| تست پایتون | `uv run pytest -q` | 16 passed |
| لینت پایتون | `uv run ruff check .` | پاس (پس از اصلاحات) |
| تایپ/بیلد فرانت | `npm run build` (tsc --noEmit + vite) | پاس |
| اجرای واقعی | `uv run zarin` + مرور همه صفحات | پاس، صفر خطای کنسول |
| موبایل | Playwright در 390×844: نمای کلی، قیف، مشتریان، همتایان، بپرس، کشوی شواهد | پاس |
| دسکتاپ | Playwright در 1440×900: همه صفحات | پاس |

## 5. بازتولید

```bash
uv run pytest -q                     # تست‌های متریک
uv run zarin                         # سرور (marts را در صورت نبود می‌سازد)
uv run python pipeline/validate.py   # QA مستقل در برابر API زنده
uv run python pipeline/audit.py      # ممیزی خام کامل
```
