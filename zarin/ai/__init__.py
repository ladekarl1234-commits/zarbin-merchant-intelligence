"""AI layer: grounded, optional, provider-abstracted.

The deterministic analytics engine is always the source of truth. Everything in
this package only *explains* numbers the engine already computed. With no API key
the whole package degrades to a no-op and the product runs fully offline.
"""
